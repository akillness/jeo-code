
[2026-06-10] P2-7 + P2-10 iteration
- P2-7: src/agent/task-tool.ts — fenceSubagentReport() wraps echoed done.reason in <<<subagent-report ... >>> DATA fence; <<< / >>> inside the report neutralized to ‹‹‹ / ›››. Test added in test/task-tool.test.ts.
- P2-10: PlanSchema/StepSchema/normalizePlanShape/parseYaml moved from src/commands/team.ts to new src/agent/plan.ts; team.ts, ralplan.ts, test/team-schema.test.ts import the shared module. team.ts no longer imports zod.
- Also this session (pre-ralph): catalog substring offsets, namespaced alias owners, reasoningEffort enum guard, gemini URL-encoding, 7 new tests, TUI glyph flake fix.
- Verified: build exit 0; 705 pass / 0 fail.
[2026-06-10] Pass 874 — 429 resilience + box-only input
- retry: escalating 429 floor (2s→30s cap), default budget 6 attempts (~60s total), onRetry delay surfaced end-to-end as AgentLoopEvents.onNotice (TUI/plain/team/subagent).
- engine: terminal LLM error reported once (doneReason only); onError removed.
- launch: boxed input footer adaptive 5–12 rows (short terminals keep the box), empty readline prompt in box mode — raw `joc>` input never visible.
- Verified: tsc 0; build ok; bun test 724 pass / 0 fail. Committed eb37122.
[2026-06-10] Pass 878 — Google OAuth bundled client secret
- google.ts: requireClientSecret() (env-only, broke login at token exchange) → googleClientSecret(): GEMINI_OAUTH_CLIENT_SECRET override → bundled gemini-cli default (b64). Test in oauth.test.ts; AGENTS.md note corrected.
- Verified: typecheck 0; bun test 732 pass / 0 fail.
[2026-06-10] Pass 879 — gjc-parity + Gemini thinking-budget + live model verification
- SKILL.md embeds (src/prompts/skills), hierarchical AGENTS.md context walk, OAuth refresh file-lock + atomic config save, streamMaxRetries consumed, picker company branding.
- gemini.ts: geminiThinkingBudget() — flash default 0, pro floor 128, pre-2.5 omitted, effort-mapped + clamped; empty MAX_TOKENS now errors (fixed live empty-reply on gemini-flash-latest).
- scripts/verify-models.ts: live per-provider recent-model verification → 0 hard failures (rate-limited models distinguished).
- Verified: typecheck 0; bun test 743 pass / 0 fail; build ok.
[2026-06-10] Ratchet loop — gjc-parity wave 1 (parallel subagents)
- Baseline: typecheck 0, 758 pass. gjc v0.4.3 surface diffed against joc → gap list.
- Wave 1 (4 parallel executors): joc state (read/write/clear/handoff over .joc/state), joc session (tmux list/attach/rm with injectable runTmux), launch -p/--print + -c/--continue + --append-system-prompt (@file), joc update (--check/--install/--json/--strict, injectable deps).
- Registry wired in runner.ts (state/session/update). 1 keep/revert event: update.ts string|null narrowing fixed at gate.
- Gate: typecheck 0; 789 pass / 0 fail (+31). Live smoke: state write/handoff/read + unknown-skill exit 1; session list; update --json all verified.
[2026-06-10] Ratchet loop — wave 2 + OAuth operability + UX fixes
- Wave 2 (3 parallel executors): launch toggles (--no-skills/--skills globs/--no-tools/--tools/--system-prompt + buildToolProtocol/filterToolMap), joc skills list|read --json, joc export --html (renderSessionHtml).
- ROOT CAUSE found by live verification: `joc chat --model X` swallowed the flag into the message → always chatted with the DEFAULT model. This masked anthropic/antigravity selection entirely ("OAuth 모델이 동작 안 함"). Fixed via parseChatArgs + test; claude-haiku-4-5 OAuth now answers live (12in/4out tokens).
- Engine: prose-salvage fix — pure-prose LLM replies become done.reason instead of bounced corrections (killed the "죄송합니다… JSON tool call" apology-as-reply bug); correction text now forbids apologies + offers done-resend; MAX_PARSE_BOUNCES=2 then salvage. New onUsage event (cumulative per-call).
- TUI: formatDuration/formatTokenCount/formatUsage (duration.ts) — "took 3 steps in 1m 45s · 14.2k in / 1.1k out tokens" summary; per-turn usage tracked via onUsage.
- Gate: typecheck 0; 831 pass / 0 fail (+42). Wave 3 in flight: plain-stream live status, gradient status animation, gemini-cli credential import (antigravity unlock).
[2026-06-10] Mock/fallback purge + TUI insight rows (pass 882)
- model-discovery: ANTIGRAVITY_MODELS static catalog fallback REMOVED. Antigravity lists models from the live Cloud Code Assist `v1internal:fetchAvailableModels` (POST) — gjc parity. Response decoded from real payload: map KEY is the callable id (entry.model is an internal MODEL_PLACEHOLDER_* enum); `agentModelSorts` groups are the API's positive agent set (preferred); non-chat role lists (tab/image/transcription/commit/mquery/deprecated-as-object) excluded data-driven. catalogOr now falls back ONLY for OpenAI/Codex.
- TUI: status split into [STEP] (metrics: step n/m · meter · elapsed · avg) / [STATUS] (live insight: current file, retry backoff) / [TOOL] (forge). Retry notices pinned to [STATUS] only — never appended to the stream log (no more repeated "rate limited" rows).
- Live proof: `joc models` → antigravity (oauth): 8 agent models (exact product picker set); `joc chat --model antigravity/gemini-3-flash` → "OK" (6 in / 95 out tokens) on dedicated Antigravity OAuth.
- Gate: typecheck 0; 864 pass / 0 fail. Keep/revert events this pass: deprecatedModelIds object-shape spread crash caught by live run and fixed + regression test.
[2026-06-10] Pass 883 — $skill 호출 + HUD phase flow + TUI 연속입력/스타일
- skills: `$스킬명 [intent]` 직접 호출(첫 토큰 정확 일치만 발동; $HOME 등은 모델로 통과) + Tab 자동완성($sp → $spec-kit) + `joc skills`에 디스커버리 경로/호출법 노출.
- TUI: finish()가 alt-screen 복귀 후 clear-to-EOL + clear-below로 잔존 행 제거 — 요약 겹침/깨진 이중 입력박스 해소, 연속 요청 입력 구조 검증(tmux 라이브 2턴). [STEP] 행에 라이브 토큰 사용량(`4.1k in / 42 out`).
- HUD: thinking→planning→executing→reporting→done phase 행(완료 체크/현재 강조/미래 dim) + [STATUS] 그룹 위아래 간격; 턴 종료시 `[DONE] thinking → … · steps · 시간 · 토큰` 정리 보고.
- 스텝/Plan 체크리스트: 완료=체크+취소선+dim, 실패=빨강, 진행=시안 볼드 (color:false 경로 바이트 동일 유지).
- Gate: typecheck 0; 883→885 pass / 0 fail. 라이브: tmux 2연속 턴 + phase report 클린 출력 확인.
[2026-06-10] Pass 881 — deep-dive gjc parity execution
- Native workflow routing in launch: `/skill deep-interview|ralplan|team|ultragoal` now hits real engines; bundled workflow docs live in `src/prompts/skills/*/SKILL.md`.
- Hook surface: opt-in `hooks.enabled` + safe pre-tool/post-turn execution via `Bun.spawn`; AGENTS hierarchy walk + configured-skill prompt advertising landed.
- Provider/model/memory: OpenAI OAuth non-Codex fail-fast, Antigravity provider/catalog wiring, company labels in `/model`·`/provider`·`/config`, token-based compaction + session compaction markers + team compaction.
- TUI/UX: footer `ctx NN%`, red-claw/blue-crab themes, in-flight abort harness (Ctrl-C cancel, ESC cancel in live TUI, second Ctrl-C hard exit).
- Verified: typecheck 0; bun test 831 pass / 0 fail; build ok; live smoke with `claude-haiku-4-5` => smoke ok.
[2026-06-10] Pass 882 — Antigravity OAuth parity + role-model UX cleanup
- Antigravity: shared Cloud Code Assist project discovery (`loadCodeAssist`/`onboardUser`), dedicated `joc auth login antigravity`, lazy projectId self-heal/persist, improved 403 guidance.
- Command surface: removed `/subagent` and `/subagents`; keep role config in `/agents` and new `/model subagent <role> [model|#N]`.
- Verified: typecheck 0; focused 96 pass / 0 fail; full bun test 858 pass / 0 fail; build ok.
[2026-06-10] Pass 883 — antigravity selectable in /model + role-pin verified
- /model·/provider·/agents·/model subagent: antigravity(OAuth 보유 시) 선택 거부 → 경고+허용으로 전환; 다른 provider는 기존 거부 유지.
- REPL 검증 테스트 추가(gemini-fallback config에서 /model 선택 + /model subagent 핀 영속); TUI stage 테스트 뷰포트 200x40 고정으로 플레이크 제거.
- Verified: typecheck 0; bun test 859 pass / 0 fail; build ok.
[2026-06-10] Pass 884 — mouse-wheel scroll TUI corruption fix
- enterAltScreen: ?1049h+?1007l (alt screen + alternate-scroll OFF) / leaveAltScreen: ?1007h+?1049l — 휠이 화살표 시퀀스로 주입돼 다음 프롬프트를 깨뜨리던 문제 제거(tmux 동일 적용).
- REPL은 매 프롬프트 전 pending tty input drain + readline 버퍼의 escape/arrow 노이즈 정리.
- Verified: typecheck 0; bun test 859 pass / 0 fail; build ok.
[2026-06-10] Pass 885 — TUI 자동 복구 (수동 키 불필요)
- 주기 리싱크: 라이브 턴 틱 25회(~3s)마다 차등 베이스라인 리셋 → 모든 라인 재기록으로 화면 오염 자동 치유.
- resize 자동 리페인트(start 등록/finish 해제), 휠 노이즈 감지 시 즉시 repaint(onNoise), raw-mode Ctrl-C(\u0003) 데이터도 abort 경로로 정합.
- Verified: typecheck 0; bun test 863 pass / 0 fail; build ok.
[2026-06-10] Pass 886 — TUI 에코 억제 및 인풋 박스 중첩 버그 수정
- runSelectPicker: 진입 시 rl.pause() + raw mode 강제 적용 / leave 시 rl.resume() 복원. 피커 작동 중 readline 입력 수신 및 에코 간섭 완벽 격리.
- GATED_OUTPUT_METHODS: _write, _writev 추가로 Bun 저수준 스트림 우회 차단.
- drawFooter: 시작/끝에 toColumn(1) 강제하여 한글 CJK 너비 오판으로 인한 커서 오프셋 밀림 차단.
- Verified: typecheck 0; bun test 871 pass / 0 fail; build ok.
[2026-06-10] Pass 887 — 실제 PTY(tmux) 동작검증 + 피커 버그 2건 추가 수정
- 검증법: 100x30 tmux PTY에서 joc launch → /model 피커 → 방향키+한글필터(너의페르소나) → capture-pane 단언.
- 발견/수정: ① rl.pause()는 피커 keypress까지 굶겨 행 위험 → 출력 게이트(previewArmed||pickerActive) 방식으로 교체, ② repaint/clear off-by-one(cursorUp(rendered)→rendered-1)로 매 키입력마다 힌트행 잔상 누적되던 실버그 수정 + 기존 행 이동은 \n 대신 CUD, ③ 피커 종료 시 rl.line/cursor 클리어(다음 프롬프트 보이지 않는 필터텍스트 누출 차단).
- 라이브 결과: 필터행 1개/잔상 0, BSpace 후 목록 복귀(행 없음), ESC 후 박스 1개 복원, /exit 정상.
- Verified: typecheck 0; bun test 871 pass / 0 fail; build ok.
2026-06-10: Executed deepinit and code-refactoring for V2. Extracted output-util and tool-registry. Lean engine.ts loop finalized. Updated AGENTS.md files.
[2026-06-11] Pass 889 — gjc-forge TUI parity wave (4 parallel executors) + inline-scrollback QA closed
- Inline tmux scrollback (888b 위에 정합): QA 하니스 캡처 버그 2건 수정 — copy-mode 뷰는 scroll_position 기반 -S/-E로 캡처, 깨끗해진 히스토리는 history-top까지 스크롤. logs/qa-inline-scrollback/runner.sh 6/6 PASSED (happy/copy-mode/no-alt/resize/burst/dedupe, tmux 3.6a).
- Welcome forge box (src/tui/components/welcome.ts): gjc 'Gajae forge' 구조 이식 — 타이틀 임베드 보더, 좌측(브랜드+태그라인+진화 아트+model/provider 필), 우측(Flow keys/Workspace/Session trail), <64col 단일 컬럼, <30col 한 줄 폴백, basename dedupe. launch 인터랙티브 배너의 평문 === 3줄 대체.
- Update Available 박스: src/util/update-check.ts(npm latest, 2.5s 타임아웃, never-throws) + src/tui/components/update-box.ts(warning 보더). 배너 직후 1.2s race로만 출력 — 라이브 프레임 오염 금지.
- Footer: cwd?(~축약, 32자 middle-truncate)+branch?(git symbolic-ref — tmux 세션 네이밍과 동일 호출) 세그먼트, ctx 세그먼트 gjc 포맷 'ctx {pct}%/{200k}'(k/M 포맷, ≥60% yellow ≥85% red, (auto) 표시). launch.ts:936 setContextUsage 라이브 공급 확인.
- Verified: typecheck 0; bun test 920 pass / 0 fail (신규 tui-welcome 11 + update-box + footer 케이스 포함); tmux QA 6/6.
[2026-06-11] Pass 890 — scrollback goal closure: README documents pass-889 surface (glyph-first ✔/✗ ledger, [STEP] ⤴ tok/s, /exit resume pointer, copy-mode wheel note); QA harness re-run on current build 6/6 PASSED (tmux 3.6a); typecheck 0, 926 pass / 0 fail; pushed.
