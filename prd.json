{
  "project": "jeo-code",
  "task": "Complete remaining code-review punch-list items P2-7 and P2-10, verify, and commit",
  "stories": [
    {
      "id": "US-001",
      "title": "P2-7: mitigate done.reason verdict spoofing in chained subagent output",
      "priority": 1,
      "passes": true,
      "acceptanceCriteria": [
        "In src/agent/task-tool.ts the subagent done.reason echoed back to the parent agent is wrapped in a fenced DATA block labelled as report data, not instructions",
        "Fence-delimiter sequences occurring inside the echoed reason are neutralized so the fence cannot be broken from inside",
        "A test in test/task-tool.test.ts asserts the fenced wrapper and delimiter neutralization"
      ]
    },
    {
      "id": "US-002",
      "title": "P2-10: extract PlanSchema + YAML parser into src/agent/plan.ts",
      "priority": 2,
      "passes": true,
      "acceptanceCriteria": [
        "src/agent/plan.ts exports StepSchema, PlanSchema, normalizePlanShape, parseYaml with behavior identical to the previous team.ts implementations",
        "src/commands/team.ts and src/commands/ralplan.ts import these from src/agent/plan.ts; team.ts no longer defines them",
        "test/team-schema.test.ts imports from the new module and still passes unchanged in substance"
      ]
    },
    {
      "id": "US-003",
      "title": "Verify and commit",
      "priority": 3,
      "passes": true,
      "acceptanceCriteria": [
        "npm run build exits 0",
        "npm test shows 0 fail with the new tests included",
        "All session changes committed with a descriptive message"
      ]
    }
  ]
}