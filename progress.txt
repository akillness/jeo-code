[2026-07-21] Pass — v0.8.37 release: gjc v0.11.5 audit continued as a LONG-TERM GOAL (batch 1: 7 more PRs triaged, 1 fixed)
- Request: "오케이 그럼 장기목표로 61개를 모두 달성해나가자" (OK, let's set achieving all 61 remaining items as a long-term goal). Created a durable goal via the goal tool (op=create) listing every one of gjc v0.11.5's remaining untriaged PR numbers as the explicit acceptance criteria — CI/test-infra PRs for gjc's OWN test suite (#2663, #2693, #2727, #2762-2769, #2773) are excluded from individual triage (recorded once as a block N/A: internal to gjc's dev process, not user-facing product behavior).
- Batch 1 — read the full PR body for 7 more candidates and triaged each with direct code evidence, not guesses:
  - #2622 "fail closed on tab worker startup" — gjc's browser tool spawns a SEPARATE native-addon-resolving tab-worker PROCESS; jeo's browser-tool.ts runs Playwright in-process with no worker-process bootstrap to fail-close on. N/A.
  - #2664 "isolate keybinding configuration arrays" — searched jeo's whole src/ for any keybinding concept: only a static, non-mutable key-HINT display (hints.ts) exists, no user-configurable keybinding array to alias. N/A.
  - #2665 "share restricted state argv classification" — hardens gjc's restricted-role-agent access to a `state` TOOL; jeo's state.ts is a human-invoked CLI command only (DEFAULT_TOOLS has no `state` entry — confirmed by reading engine.ts's full tool dispatch table), so a subagent can never even attempt to call it. N/A.
  - #2670 "resource-gc: advance earlier sweep deadlines" — gjc tracks MULTIPLE independently-deadlined cleanup policies behind one shared re-armable timer (the bug: a new EARLIER deadline didn't preempt it); jeo's process-reaper.ts is a single global periodic sweep with no per-policy deadline tracking at all, so the specific race is structurally impossible. N/A.
  - #2669 "bound malformed compatible responses" (web-search) — verified LIVE (not just read): fed a wrong-shaped-but-valid-JSON body (`output` as a string instead of an array) directly through jeo's own parseOpenAISearchResponse/parseGeminiSearchResponse (added in the v0.8.31 pass) — neither threw; both degraded gracefully via existing `?? []`/`Array.isArray()` guards. Already correct, confirmed by direct execution, not code-reading alone.
  - #2652 "recover malformed Round 0 intent asks" — gjc's own multi-turn "deep-interview Round 0" AskTool wire-schema/coordinator state-machine mismatch; jeo's deep-interview.ts has no equivalent wire-schema-vs-runtime-validation surface of this specific shape. N/A.
  - #2658 "safely attach multiple pasted image paths" — gjc's version is heavily hardened against symlink/hardlink/TOCTOU/remote-file:///consent attacks for a fundamentally different threat model (arbitrary bracketed-paste text from a possibly-untrusted clipboard source, attaching up to 16 images with a required consent step). jeo's src/util/file-attachment.ts is a much simpler single-path-in-composer-text flow with NO size limit at all before reading the full file into memory — a genuine, scoped-down, real gap: fixed by adding a `stat()`-before-`readFile()` check rejecting non-files or anything over 25 MiB (MAX_ATTACH_IMAGE_BYTES), avoiding an unbounded-memory-read DoS from a stray/huge dropped path. Did NOT port the FULL gjc hardening list (symlink/TOCTOU/consent) since jeo's narrower threat model (user directly types/drops their OWN path) plus the ALREADY-EXISTING magic-byte validation gate (attachmentFromImageBytes rejects non-image bytes) already close the most severe exfiltration vector that hardening targets.
- test/file-attachment.test.ts: +1 test — writes a REAL 25MiB+1KiB oversized file and a real small PNG to a temp dir, drives BOTH through the real (non-injected) filesystem reader, confirms the oversized one is rejected before ever being attached and the small one still attaches normally through the identical code path.
- CHANGELOG.md [0.8.37]; package.json 0.8.37; progress.txt entry. Goal status: 11 of ~65 v0.11.5 PRs now triaged (2 fixed: #2591, #2658; 9 N/A with evidence: #2667, #2749, #2721, #2622, #2664, #2665, #2670, #2669, #2652). ~50 remain (minus the ~11 CI/test-infra block), tracked via the active goal for subsequent passes.
- Verified: bun run typecheck clean; full bun test 3120 pass / 0 fail across 307 files.
[2026-07-21] Pass — v0.8.36 release: first PR-by-PR audit of gjc v0.11.5 (stdin UTF-8 corruption fix; audit honestly incomplete)
- Request: "gjc 0.11.5까지의 모든 업데이트 내용 반영된거맞아?" (is it confirmed that everything up through gjc 0.11.5 has been applied?). Honest answer given directly: NO, not fully verified — prior passes used release-note prose (v0.10.2-v0.11.4) or direct-source structural diffs (v0.11.5's provider catalog, tool surface) rather than reading v0.11.5's actual ~65-entry "What's Changed" PR list, which is the category most likely to miss BEHAVIOR fixes (no new file to structurally diff against).
- Fetched v0.11.5's real release body (65 PRs) for the first time and triaged every one. Read the 3 most promising candidates' full PR bodies (#2667 read-tool internal URL selectors, #2591 TUI supplementary-Unicode input, #2749 team worker-pane targeting) before deciding scope: #2667 is about gjc's OWN internal `agent://`/`artifact://`-style resource URI scheme, which jeo's `read` tool does not have an equivalent of (confirmed via search — no "internal://" concept in jeo's tools.ts) — N/A. #2749 (team stable-window pane targeting) needs deeper investigation into jeo's team.ts tmux wiring — not yet done this pass.
- #2591 ("preserve supplementary Unicode input") WAS directly applicable and confirmed as a real, live bug: `src/commands/launch.ts`'s `kfDataHandler` did `chunk.toString("utf8")` on EACH raw stdin `Buffer` chunk independently — a chunk boundary landing mid multi-byte UTF-8 sequence (real terminals/ptys never guarantee byte-aligned delivery) produces the replacement character (U+FFFD) on BOTH sides of the split instead of the real character, corrupting Korean/CJK IME text, emoji, or any non-ASCII input whenever this happens. Quantified live before fixing: a 4-byte emoji split after 2 bytes decoded to two garbage replacement characters, never the original. Fixed with a persistent `node:string_decoder` StringDecoder (the standard streaming-UTF-8 fix, exactly what Node's own string-mode streams do internally) — buffers an incomplete trailing sequence across chunks, only emits complete characters. `filterPromptInputChunk`'s own separate ASCII-level carry logic (paste markers, CRLF splits) is unaffected — the two are complementary.
- test/stdin-utf8-decode.test.ts: new file, 5 tests — quantifies the OLD bug directly (a real split 4-byte emoji really does decode to two U+FFFD), proves the fix reassembles both a split emoji and a split 3-byte Hangul character, confirms ASCII-only chunks are unaffected, and asserts the real source wiring (not a naive toString).
- Real live verification: booted a real `jeo --tmux` session, typed real Korean+emoji text via `tmux send-keys` (real UTF-8 bytes over a real pty) — rendered correctly, no corruption.
- CHANGELOG.md [0.8.36] explicitly documents the audit as INCOMPLETE (not overclaiming): only this one item confirmed so far out of v0.11.5's ~65 PRs; still-untriaged candidates flagged for follow-up include team.ts worker-pane targeting (#2749), secret-detection regex hardening (#2715/#2725), standalone HTML export encoding hardening, retry stream-timeout defaults (#2721), subagent resume failure surfacing (#2722/#2728/#2731), and tool-discovery persistence across session resume (#2674/#2702).
- Continued the audit after publishing 0.8.36 (same session, same context): #2749 ("team: target stable window for worker panes") — read jeo's actual `src/commands/team.ts` in full: `jeo team` is architecturally NOT gjc's feature at all — it has NO tmux pane-splitting/worker-pane concept whatsoever; parallel plan steps run via git worktrees + async `SubagentRegistry`/`runSubagentOnce`, never a visible tmux split. Confirmed N/A with direct evidence, not a guess. #2721 ("retry provider stream timeouts under bare default config") — read jeo's `src/util/retry.ts`'s `defaultRetryable()` in full: it is a PURE predicate over the error itself (message-text/status matching), with NO "legacyRetryConfigured"-style gate requiring explicit `retry.*` config keys before engaging — the exact indirection layer gjc's bug lived in does not exist in jeo's design, so the bug class cannot occur here. It already explicitly classifies "stream idle"/"no chunk"/generic "timeout" text as retryable while deliberately excluding the hard overall-deadline wall-clock cap (comments in that file show this was already gjc-informed from an EARLIER parity pass, well before this session). Confirmed correct-by-architecture, no action needed.
- Full status after this session's audit: 4 of v0.11.5's ~65 PRs concretely investigated with direct evidence (1 fixed — #2591; 3 confirmed N/A with reasons — #2667, #2749, #2721). The remaining ~61 are NOT individually verified yet, though most fall into categories already established as architecturally inapplicable across many prior passes (gjc's SDK-broker session-index/resource-GC/self-reap, Hindsight memory, ACP, plugin marketplace cache, MCP OAuth discovery, multi-process session ACLs, Gajae Pet, ci/test infra, Windows-specific resident-cache paths). Told the user directly and honestly that the audit is NOT 100% exhaustive rather than claiming full v0.11.5 coverage.
- package.json 0.8.36; progress.txt entry.
- Verified: bun run typecheck clean; full bun test 3119 pass / 0 fail across 307 files (one unrelated timing-sensitive test flaked under full-suite parallel load on the first run, confirmed pre-existing via isolated pass, then a clean full re-run came back 0 fail).
[2026-07-21] Pass — v0.8.35 release: calc tool added after a deeper structural gjc-parity sweep
- Request: "https://github.com/Yeachan-Heo/gajae-code 다시 면밀히 파악하고 적용해줘" (examine gajae-code again, more thoroughly, and apply). A `playwriter` (running-browser automation) skill's full SKILL.md was attached to this turn's context, but it is unrelated to this task — reading a public GitHub repo's source for a structural comparison needs `git clone`/`read`, not live-browser automation, so the skill was correctly not invoked.
- Re-cloned the real, public `Yeachan-Heo/gajae-code` (`git clone --depth 1`) — no new release since v0.11.5 (already the current latest, confirmed last pass too) — but went DEEPER this time: instead of re-checking release notes or the provider catalog again, structurally compared gjc's actual TOOL implementations (`packages/coding-agent/src/tools/`, ~70 files) against jeo's registered tool surface (`DEFAULT_TOOLS` in engine.ts).
- Found one clean, safely-portable, previously-uncovered gap: gjc's `calculator.ts` (a self-contained tokenizer + recursive-descent parser + evaluator, zero gjc-specific dependencies in its core logic) — jeo had no arithmetic-evaluation tool at all. Ported faithfully to `src/agent/calc-tool.ts`: same precedence/associativity (+/- lowest, then */%,  then unary, then right-associative ** highest before parens/literals), same literal support (decimal, hex 0x, binary 0b, octal 0o, scientific notation), same -0 normalization and non-finite-result rejection — verified against gjc's actual source read directly, not inferred. Rewired for jeo's own `ToolResult`/tool-registration conventions instead of gjc's bespoke TUI `Component`/`Theme` renderer (not applicable here).
- Wired end-to-end, not just as an isolated function: `DEFAULT_TOOLS.calc` + `READONLY_TOOL_NAMES.calc` in engine.ts, a full native function-calling schema in tool-schemas.ts (noticed gjc's own `bisect`-equivalent tool has NO native schema on that path — an existing, unrelated gap in jeo's own bisect wiring left as-is/out of scope — but gave `calc` a complete schema since its params are simple enough that skipping it would be a needless regression from the quality bar the read/write/bash schemas already set), a new numbered line in BOTH `TOOL_PROTOCOL` (tool 19, renumbering `done` to 20) and the read-only-subagent `READONLY_TOOL_PROTOCOL` (tool 8, renumbering `done` to 9) — confirmed `calc` needs no extra subagent-toolset wiring since `subagentToolset()` filters by an explicit MUTATING blacklist that calc (having no side effects) was never going to be caught by.
- Compared the REST of gjc's tools/ directory against jeo's surface before deciding scope, not just picking the first easy win: `archive-reader`/`sqlite-reader` already covered by jeo's own unified `read` tool; `image-gen` already covered by `generate_image`; `ast-edit`/`ast-grep`/`irc`/`job`/`subagent`/`todo-write`/`skill`/`debug` already ported under matching names in earlier passes. Explicitly deferred (real, separately-scoped features, not one-file ports): `gh` (GitHub CLI integration), `ssh` (remote execution), `cron` (scheduled tasks), `render-mermaid` (diagram rendering), `checkpoint` (git-based undo), `json-tree`, `monitor`, `ask` (structured mid-turn Q&A), `vim` (a full Vim-mode composer — 6 source files in gjc, a genuine standalone feature).
- test/calc-tool.test.ts: new file, 12 tests — precedence/associativity (incl. right-associative ** and left-associative -//), unary chaining, hex/binary/octal/scientific literals, -0 normalization, syntax/empty/non-finite error paths, tokenizer shape, and END-TO-END wiring assertions against the REAL DEFAULT_TOOLS.calc, nativeToolSchemasFor, both TOOL_PROTOCOL texts, and subagentToolset (not mocks).
- CHANGELOG.md [0.8.35]; package.json 0.8.35; progress.txt entry.
- Verified: bun run typecheck clean; full bun test 3114 pass / 0 fail across 306 files (was 3102/305, +12 new).
[2026-07-21] Pass — v0.8.34 release: provider-catalog sync against gjc's real current source (deepinfra, litellm added; tencent/system-prompt/route preserved)
- Request: "시스템 프롬프트의 철학과 route 명령어 기능은 유지하고 나머지는 모두 gjc 최근 버전의 기능으로 싹다 바꿔줘. tencent 프로바이더도 유지해줘" (keep the system prompt's philosophy and the /route command, replace everything else with gjc's latest-version features, and also keep the tencent provider). Literally "replace everything else" is not a coherent, safely-executable single task — jeo is a deliberately single-process, pure-TypeScript, zero-native-dependency CLI (reaffirmed across 30+ prior gjc-parity passes), while gjc's current source is a large monorepo with Rust natives, an SDK-broker architecture, IRC/Telegram/Discord bots, coordinator-mcp, and more — replacing that architecture wholesale would mean discarding jeo's own foundational identity, not "changing a feature." The explicit tencent callout was the concrete, disambiguating signal: this pass treats the request as a provider/model-catalog sync (the bounded, verifiable domain gjc's own naming actually confirms tencent doesn't currently exist in), while declining to fabricate a wholesale architecture replacement.
- For the first time in this repo's gjc-parity history, did a DIRECT source comparison instead of inferring from release notes/PR bodies: `git clone --depth 1 https://github.com/Yeachan-Heo/gajae-code.git` (the repo is genuinely public) and read `packages/ai/src/models.json` directly — gjc's real, current (v0.11.5) provider list. Confirmed: gjc's list has NO "tencent" entry at all (explains the user's explicit callout), and has ~17 providers jeo's catalog lacks.
- Added `deepinfra` (`https://api.deepinfra.com/v1/openai`, DEEPINFRA_API_KEY) and `litellm` (`http://localhost:4000/v1` — LiteLLM's own local-proxy default, self-hosted so deliberately has NO knownModels list unlike every other row, since the model catalog behind it is entirely user-configured) to `src/ai/providers/openai-compatible-catalog.ts` — ONE table row each, auto-registered via the existing generic `register-providers.ts` loop, no new adapter code. `ProviderName` (ai/types.ts) and `AuthProvider`/`API_KEY_ONLY_PROVIDERS` (auth/storage.ts) gained both names.
- Cross-referenced the REST of gjc's new list against jeo's existing catalog before deciding what to add, rather than adding indiscriminately: `moonshot` (gjc) and jeo's existing `kimi` provider share the EXACT SAME base URL (https://api.moonshot.ai/v1) — gjc split OAuth-subscription vs. API-key into two provider identifiers where jeo intentionally keeps one; `firepass` (gjc) and jeo's existing `fireworks` share the EXACT SAME base URL (https://api.fireworks.ai/inference/v1) — already at parity under different names, confirmed by direct byte-for-byte URL comparison, no action needed for either.
- Explicitly did NOT port (with reasons, not silence): `opencode`/`opencode-go`/`opencode-zen` (competing agent's own hosted proxy product — a jeo user would need an opencode account; low practical value); `azure-openai`/`amazon-bedrock`/`google-vertex`/`cursor`/`github-copilot`/`gitlab-duo` (each needs a genuinely new, non-trivial auth/wire adapter — Azure resource config, AWS SigV4 signing, GCP OAuth+region, Cursor's undocumented proprietary API, GitHub Copilot device auth, GitLab OAuth — none fit the existing "base URL + API key" row pattern; fabricating these without live verification would violate the no-fabrication contract); `cloudflare-ai-gateway`/`vercel-ai-gateway` (need a per-account/gateway URL embedded in the base — not a fixed-row fit, already achievable via jeo's existing `provider add --base-url` custom-endpoint feature); `ollama-cloud` (jeo's `ollama.ts` adapter never sends an Authorization header by design for the keyless local daemon — serving the hosted https://ollama.com variant needs a real adapter change beyond a catalog row, deferred as a scoped follow-up rather than rushed).
- System prompt (`src/prompts/`) and the `/route` command (`src/commands/launch/route-slash.ts`, `src/agent/route-history.ts`) were not touched at all, per the explicit preservation instruction.
- test/deepinfra-litellm-provider.test.ts: new file, 5 tests (registration/env-var/base-URL, id routing, credential-gated readiness for both new providers) — first attempt asserted `qualifyModelId` would strip the provider prefix like tencent's test does; caught and corrected the assertion once `describeProvider`'s actual (correct) behavior showed tencent's models are additionally catalogued as bare ids in model-catalog.ts (unlike deepinfra/litellm, matching the un-catalogued fireworks/together/groq precedent), so qualifyModelId legitimately prefixes rather than no-ops for the new providers.
- CHANGELOG.md [0.8.34]; package.json 0.8.34; progress.txt entry.
- Verified: bun run typecheck clean. First full bun test run hit many unrelated failures (browser-tool, daemon-command, tokenizer, tmux, search-gitignore, rate-limit) under heavy concurrent system load (94 bun/node processes observed) — none in subsystems this pass touched (pure TS provider-catalog data + type unions cannot plausibly break browser/daemon/tokenizer tests); confirmed via isolated re-runs of a representative sample (all passed standalone), then a clean full re-run came back 3102 pass / 0 fail across 305 files (was 3097/304, +5 new).
[2026-07-20] Pass — v0.8.33 release: real terminal-resize live testing closes a progressive screen-corruption bug (idle footer + mid-turn renderer)
- Request: "tui 해상도 부분 적용테스트 실동작으로 진행하고, 아직도 해상도 조정시 고정된 부분이 깨지는 문제가있는데 개선적용해" (run the TUI-width fix's real-world verification, and there's still a problem where already-settled parts break when resizing — investigate and fix). Direct continuation of the 0.8.32 grapheme-cluster width pass, this time driving a REAL `jeo --tmux` session with real tmux `resize-window` calls and real keystrokes rather than static unit fixtures.
- Live repro methodology: booted `jeo --tmux` in a real detached tmux session (own harness modeled on `scripts/tmux-verify.sh`), resized the window down (80x24 -> 20x10, later 90x26 -> 55x18 mid-turn), and captured the pane. Found real, visible corruption: the idle-prompt footer's status-bar line DUPLICATED once per keystroke, growing into a taller and taller stack of `⬢ model … ▦ ctx%` lines. Root-caused it DETERMINISTICALLY and outside jeo/tmux timing entirely: monkey-patched `process.stdout.write` to log every raw byte sequence jeo emitted, then replayed the EXACT captured bytes via `cat` into a fresh, jeo-free tmux pane — the SAME duplication reproduced from the bytes alone, proving the bug lives in what jeo emits, not in readline/timing/interaction.
- Binary-searched the captured write sequence (replaying an increasing prefix of writes into fresh panes) to find the exact write that introduces the extra duplicate line, then measured its stripped (ANSI-free) content width directly: the footer/status-bar row was 24 columns wide on a 20-column terminal. Traced to `src/tui/components/status.ts`'s `renderStatusBar`: `const cols = Math.max(24, Math.trunc(d.cols))` — clamped UP to a fixed floor of 24 regardless of the REAL terminal width (the exact same anti-pattern as `input-box.ts`'s `renderInputFrame`, which had `Math.max(24, ...)` too, and a THIRD, deeper internal floor inside `renderStatusBar` itself: `leftBudget = Math.max(8, cols - rightWidth - ...)`). A real terminal hard-wraps that overflow, and the idle-footer's OWN row-parking bookkeeping (`footerParkedRow`/`footerRendered`) has no way to account for a wrap the terminal performed on its own — every subsequent keystroke's repaint then starts one row further off from where the code assumes, growing the duplicate stack by exactly one line per keystroke.
- Also found and fixed a related but SEPARATE bug in `src/tui/components/welcome.ts`'s `cols < 30` narrow fallback (introduced by MY OWN 0.8.32 fix, which capped it to `cols` instead of the established `cols - 1` "leave the last column free" convention used everywhere else in that file) — a line filled to the EXACT terminal width followed by its own trailing `\n` is ambiguous to real terminals (pending-autowrap vs. explicit newline), which independently reproduced the identical duplicate-line symptom even after the overflow itself was already fixed. Caught this by literally counting the stripped welcome-line's character count against `cols` in the captured bytes (exactly `cols`, not `cols-1`) before assuming the earlier fix was complete.
- Fixed all three floors (`status.ts`'s `cols` and `leftBudget`, `input-box.ts`'s `cols`) to clamp to a minimum of 1 instead of 24/8, and `welcome.ts`'s fallback to `cols - 1`. Re-verified live via the SAME tmux resize+keystroke reproduction: zero duplication across repeated keystrokes at both a moderate (55x18, mid-turn) and an extreme (20x10, idle) narrow width.
- Separately, while investigating, found and fixed an INDEPENDENT, deterministically-reproducible bug in `src/tui/renderer.ts`'s reserve-mode resize path (used by the mid-turn `LaunchTui`/`Renderer` live frame, a different code path from the idle-prompt footer): `render()`'s resize branch called `clear()` unconditionally, which zeroes `coverRows`/`prev` as an independent write, fooling the SAME call's own row-reservation block into believing nothing occupies the screen and spuriously emitting real `"\n"` characters (a genuine terminal scroll) even when the post-resize frame was SHORTER than the pre-resize one. Reproduced with a standalone `Renderer` unit harness (no tmux at all) before fixing; the fix reuses the same `reset()`-based bookkeeping the periodic self-heal resync already relies on, applied ONLY in reserve mode (non-reserve/alt-screen mode keeps its original defensive `clear()`, confirmed via a dedicated regression test that it's unaffected).
- test/status-bar.test.ts, test/input-box.test.ts: +1 test each (never overflow a realistically narrow 5-23 col terminal). test/tui-welcome.test.ts: existing narrow-fallback test corrected from asserting the old (buggy, exactly-`cols`) 25-char value to the fixed `cols-1`=24-char value, +1 new never-overflow sweep. test/tui-renderer.test.ts: +2 tests (reserve-mode resize-down never scrolls; non-reserve mode keeps its ED clear).
- CHANGELOG.md [0.8.33]; package.json 0.8.33; progress.txt entry.
- Verified: bun run typecheck clean; full bun test 3097 pass / 0 fail across 304 files. Real (unmocked) tmux verification for both the idle-prompt footer and the mid-turn LaunchTui/Renderer path, at multiple resize targets, confirming no progressive duplication remains — the one residual, honestly-not-claimed-fixed artifact is a single already-historical-scrollback line (a user-message box printed BEFORE a resize) getting reflow-split by tmux itself, which is an inherent, universal terminal/multiplexer limitation for content already scrolled into history, not something fixable at the app layer without an alt-screen architecture change (out of scope here) — it does not recur or worsen, unlike the fixed bugs.
[2026-07-20] Pass — v0.8.32 release: grapheme-cluster-aware TUI width (gjc Korean/emoji wrap-corruption parity)
- Request: "tui 해상도깨지는 부분도 gjc 의 tui 를 적용해" / "깨지지 않도록 적용하는거야" (apply gjc's TUI approach to the parts where [layout] breaks, so it doesn't break) — continuing the standing gjc-inheritance chain into TUI rendering correctness.
- Investigated gjc's TUI release history for the "breaking" class explicitly: PR #1996 "fix(tui): preserve Korean assistant prose wrapping" (Korean/Jamo tone marks + "VS16 emoji presentation, ZWJ, keycaps, and emoji modifiers" — gjc's own native Rust `UnicodeWidthStr`/grapheme engine). Cross-checked jeo's own `charWidth`/`visibleWidth` against each named case: Korean/Hangul/CJK ranges were ALREADY correct (verified numerically — Hangul Compatibility Jamo 0x3130-0x318F, Hangul syllables 0xac00-0xd7a3, etc. all already in jeo's wide-range table); CRLF-as-zero-width (gjc #2036) was ALREADY correct too (CR/LF are ASCII control chars < 32, already width-0 in `charWidth`). The genuinely missing piece: jeo's `visibleWidth`/`truncateToWidth` summed width PER CODE POINT, which is wrong for any multi-code-point sequence a terminal renders as ONE glyph.
- Quantified the real bug before fixing (not asserted): naive per-code-point sum vs. what a terminal actually renders — ❤️ (heart+VS16) 1 vs 2, 👍🏽 (thumbs-up+Fitzpatrick modifier) 4 vs 2, a 4-person ZWJ family emoji 8 vs 2, a digit keycap 1 vs 2. The family-emoji case (off by 6 columns) is a severe, visible box-border/wrap misalignment the instant such a sequence appears in any message — exactly the "깨짐" class the user named.
- src/tui/components/width.ts: new `nextGraphemeCluster(s, i)` collapses VS15 (text presentation, width 1)/VS16 (emoji presentation, width 2), Fitzpatrick skin-tone modifiers (U+1F3FB-U+1F3FF, absorbed at 0 extra width), keycap combiners (U+20E3, width 2), trailing combining marks, and ZWJ-joined chains (recursive — an arbitrary-length ZWJ chain collapses to width 2, matching how virtually every modern terminal renders it as one glyph) into ONE atomic {length, width} unit. `visibleWidth` and `truncateToWidth`'s slow paths now consume this instead of raw `charWidth(codePointAt(i))` — `wrapTextWithAnsi` inherits the fix for free since it's built on both. `truncateToWidth` also gained cluster-atomicity at the truncation boundary itself (a 2-wide cluster is now dropped whole rather than potentially split mid-sequence, a second corruption vector the old per-code-point cut was exposed to). Pure-TS, no native dependency (jeo has none) — a deliberately pragmatic subset of UAX #29 scoped to the sequences a chat/coding TUI actually sees, not a general script-shaping engine.
- test/width.test.ts: +2 tests — `nextGraphemeCluster` unit coverage for all 4 sequence classes above (plus a plain-astral-emoji "unaffected" control case), and a `truncateToWidth` atomicity test proving a 2-wide cluster is dropped whole (never half) at a too-small budget.
- Real verification beyond unit tests: (1) ran the OLD per-code-point sum (via `charWidth` over a `for...of` char iterator, still reproducible standalone) against the NEW `visibleWidth` for all 4 sequence classes via `bun -e`, confirming the exact before/after delta quoted above — not inferred; (2) wrapped a real mixed Korean/emoji message (family emoji + VS16 heart + skin-tone thumbs-up) into a live 20-column box via `wrapTextWithAnsi`/`visibleWidth`, confirming every wrapped line's real display width fit the declared budget with correct padding end-to-end.
- CHANGELOG.md [0.8.32]; package.json 0.8.32; progress.txt entry.
- Verified: bun run typecheck clean; full bun test 3092 pass / 0 fail across 304 files on a clean re-run (an initial run hit 2 transient parallel-load flakes — unrelated /model-pin-routing and a Node --inspect-brk debug-session test, neither touching width.ts — both confirmed pre-existing/isolated-pass, not caused by this change).
[2026-07-20] Pass — v0.8.31 release: native OpenAI + Gemini web search (no more DDG-only fallback for non-Anthropic models)
- Request: "폴백되지않고 실제 동작하도록 개선해줘" (make it actually work instead of falling back) — direct follow-up to the prior turn's finding that web_search only has a native Anthropic backend; every other model provider (openai, gemini, xai, ollama, ...) silently degraded to keyless DuckDuckGo HTML scraping even when the session had real OpenAI/Gemini credentials.
- Verified real, current wire formats via primary sources before writing any code (no fabrication): platform.openai.com/docs/guides/tools-web-search for the Responses API `web_search` hosted tool (output items: `web_search_call`.action.query, `message`.content[].annotations[].url_citation); ai.google.dev/gemini-api/docs/google-search PLUS the current `google-genai` Python SDK's raw types.py (fetched and grepped directly — `class GroundingMetadata`/`GroundingChunk`/`GroundingSupport`/`GoogleSearch` at lines 7559/7305/7415/4447) to confirm the classic `generateContent` endpoint's `groundingMetadata`/`groundingChunks`/`groundingSupports`/`webSearchQueries` grounding schema is STILL current (Google's docs site now mostly showcases the newer, unrelated "Interactions API" — the classic generateContent-based schema needed independent confirmation from the SDK source, not just the docs page, to avoid shipping a stale/wrong shape).
- src/agent/web-search.ts: added `searchOpenAI`/`parseOpenAISearchResponse` (api.openai.com/v1/responses, `tools:[{type:"web_search"}]`) and `searchGemini`/`parseGeminiSearchResponse` (generateContent, `tools:[{googleSearch:{}}]`). Both gated on a REAL API key ONLY — deliberately reject OAuth: OpenAI's ChatGPT/Codex OAuth backend (chatgpt.com/backend-api/codex/responses) is a separate undocumented surface never verified to support hosted Responses tools, and Gemini/Google OAuth is scoped to the Cloud Code Assist/antigravity backend and doesn't serve the plain generativelanguage.googleapis.com endpoint at all (per model-manager.ts's existing oauthServesModel contract) — shipping an OAuth path for either would have been a guess, not a verified fix. New env overrides: JEO_SEARCH_API_KEY_OPENAI/JEO_SEARCH_MODEL_OPENAI, JEO_SEARCH_API_KEY_GEMINI/JEO_SEARCH_MODEL_GEMINI. MODEL_PROVIDER_TO_SEARCH gained openai→openai, gemini→gemini, antigravity→gemini (only qualifies with a real GEMINI_API_KEY, never its own OAuth).
- test/web-search.test.ts: +9 tests — response-parsing fixtures for both new providers, live-mocked-fetch tests asserting the EXACT outgoing request (URL/headers/body) for both, provider-chain routing tests (openai/gemini/antigravity route to their OWN native provider only when THEIR OWN credential is set, confirming they never borrow another provider's key), and no-API-key→DuckDuckGo fallback tests for both.
- Real verification beyond mocks: this sandbox has no OpenAI/Gemini API keys configured (checked directly — providers.openai/providers.gemini both false, env keys unset), so a live end-to-end call against the new native endpoints wasn't possible here; instead ran the REAL (unmocked) `webSearchTool` end-to-end via `bun -e`, which genuinely hit DuckDuckGo over the network and returned real, current TypeScript 5.9 release-note results — confirms the whole provider-chain plumbing (including the new gating logic) works live, not just under test doubles.
- CHANGELOG.md [0.8.31]; package.json 0.8.31; progress.txt entry.
- Verified: bun run typecheck clean; full bun test 3090 pass / 0 fail across 304 files (3081 baseline + 9 new).
[2026-07-20] Pass — v0.8.30 release: fix silently-broken `bun run build` (compiled binary parity gap)
- Request: "별도작업 조사해서 개선해줘" (investigate and fix the separate issue) — the `bun run build` failure flagged as pre-existing/unrelated during the 0.8.29 live-verification pass.
- Root cause traced fully: playwright-core@1.61.0's bundled `coreBundle.js` has a lazy `require("chromium-bidi/lib/cjs/...")` reachable only via its BiDi bridge (a code path jeo never takes — `browser-session.ts` uses plain CDP). Confirmed against the REAL published npm metadata (registry.npmjs.org/playwright-core/1.61.0) that playwright-core genuinely ships ZERO declared dependencies (everything else is bundled) — chromium-bidi is deliberately NOT installed, and Bun's `--compile` bundler statically resolves every reachable `require()` (dead code path or not), so the plain `package.json` `"build"` script always crashed. `scripts/ci-release-build-binaries.ts`'s `buildCommand()` already had the fix (`--external chromium-bidi` + `--keep-names` + `--no-compile-autoload-dotenv`, with a comment explaining exactly this) — the dev-only `build` script was just never updated to match, a classic parallel-convention drift.
- package.json: `"build"` script brought into sync with buildCommand()'s three safety flags.
- test/release-binaries.test.ts: the existing buildCommand test gained 2 assertions locking `--external chromium-bidi` (previously unlocked despite existing in source); new test reads package.json at runtime and asserts every flag buildCommand() emits (minus target-specific --target/--outfile) is present in the plain build script, so the two commands can never silently re-drift.
- Real verification (not just unit tests): `bun run build` now genuinely compiles (`bundle 257 modules` / `compile dist/jeo`, 86MB Mach-O arm64 standalone binary); ran the COMPILED binary directly (not `bun src/cli.ts`) — `./dist/jeo --version` → `jeo v0.8.30` exit 0, `./dist/jeo --help` → full command listing exit 0. dist/ is gitignored, cleaned up after verification.
- package.json 0.8.30; CHANGELOG.md [0.8.30] entry; `bun run changelog:sync` mirrored into all four READMEs; fast-forward merged into `main` (linear history, zero conflicts).
- Verified: `bun run typecheck` clean; full `bun test` 3081 pass / 0 fail across 304 files (3080 baseline + 1 new).
[2026-07-20] Pass — v0.8.29 release: gajae-code (gjc) v0.10.2->v0.11.4 gap analysis
- Request: "jeocode 에 gjc 의 기능 적용해줘" (apply gjc's features to jeo-code) — continue the standing gjc-inheritance chain past the last-analyzed v0.10.1 (0.8.26 pass) against gjc's now-current v0.11.4.
- Pulled all 6 intervening GitHub releases (v0.10.2, v0.11.0, v0.11.1, v0.11.2, v0.11.3, v0.11.4) via the GitHub API (release bodies + linked PR bodies for the two ambiguous ones), not the release-page HTML. Verdict: the large majority is gjc-specific architecture jeo intentionally does not replicate — Gajae Pet terminal overlay, SDK broker/session-index corruption recovery + `gjc gc --repair-session-index`, coordinator-mcp, Telegram/Discord rich-delivery + inbound-lease internals, browser tab workers, IRC, RPC durable default-model selection, Windows psmux, the `gjc worktree`/`wt` subcommand removal (jeo never had that subcommand — its `--worktree` flag is a different, already-shipped feature), Kimi Coding Plan preset->K3 relabel (jeo has no "Coding Plan Eco/Medium/Pro" preset tier to relabel).
- Investigated the two candidates that looked jeo-shaped and pulled their full PR bodies:
  - gjc #2586 "fail fast on empty non-TTY stdin": read launch.ts's one-shot gate (`isOneShot = flags.print || joinedArgs.length > 0 || !process.stdin.isTTY`) — it already forces one-shot whenever stdin isn't a TTY (including `isTTY === undefined`, since `!undefined` is truthy), independent of arg length, then bails with "No input provided." on empty piped stdin. The hang gjc's PR fixed (an empty non-TTY stdin falling through to interactive TUI launch) cannot occur in jeo's control flow — ruled out with direct evidence, no action.
  - gjc #2629 "fix(tui): suppress autocomplete in backtick spans" (closes gjc #2619): genuinely applicable and closed. `src/tui/components/autocomplete.ts`'s `complete()` mid-line branches (`$skill`, `/command` mention-style matching) only ever looked at the trailing whitespace-delimited token, never the line's backtick-span state — so plain prose containing a not-yet-closed literal backtick (or a backtick-quoted command example like "use `` `/model` `` to switch") could pop the live command/skill dropdown and ride into `readlineCompleter`'s Tab dispatch mid-sentence. New `insideBacktickSpan(line, pos)` walks the line up to the token's start offset with escape-parity (`\`` never toggles the span — verified against a naive non-escape-aware count, which would have wrongly treated an escaped+real backtick pair as closed/even). `$skill`/`/command` branches now check it before matching; `@path` mentions are explicitly UNAFFECTED (checked before the new gate), matching gjc's split of "suppress command/skill, preserve path completion in literals." Closed/paired spans and span-free lines are unaffected.
- test/autocomplete.test.ts: +1 test, 6 assertions (open span suppresses /command and $skill; closed/paired span doesn't; escape-parity distinguishes an escaped backtick from a real toggle; span-free lines are untouched; @path stays live inside a span).
- CHANGELOG.md [0.8.29] entry; package.json 0.8.29; `bun run changelog:sync` mirrored the latest 5 entries into all four READMEs.
- Verified: `bun run typecheck` clean; full `bun test` 3080 pass / 0 fail across 304 files (3079 baseline + 1 new).
[2026-07-14] Pass — v0.8.26 release: haiku catalog cleanup + gajae-code 0.8.0-0.10.1 gap analysis
- Request: compare jeo-code against akillness's earlier Fable-5 blog post analysis (prior session, archived), then remove sub-4.6 Anthropic claude-haiku-4-5 from the exposed model catalog/aliases per user direction, apply it, run real-world verification, and deploy; then investigate gajae-code's (gjc) recent 0.8+ updates and apply/verify/deploy any genuinely applicable ones.
- Haiku removal: claude-haiku-4-5 dropped from src/ai/model-catalog.ts (5 remaining Anthropic rows, all >=4.6), the `haiku` BUILTIN_ALIASES entry (src/ai/model-registry.ts, src/agent/config-schema.ts's local copy), and web-search.ts's DEFAULT_SEARCH_MODEL (-> claude-sonnet-4-6). 27 tests broke across 6 files (test/model-catalog.test.ts, model-manager.test.ts, model-provider-mapping.test.ts, launch-prompt-routing.test.ts ×22, launch-fallback-live-discovery.test.ts) — every new expected value was independently derived by tracing the REAL tierModelPool/equivalentRouteFallback/selectFromPool logic (an eval harness computed exact pool compositions before writing assertions, not inferred from titles), not just pattern-matched from old->new. Two tests needed a genuinely new `gemini` provider credential added to preserve their original "switches to an equivalent" intent (the trivial-tier pool would otherwise be empty with only Anthropic credentialed, silently falling back to defaultModel and defeating the test's purpose).
- gjc gap analysis: read all 16 GitHub release notes (v0.8.0->v0.10.1, ~200 PRs total) via the GitHub API, not just the release page HTML (which had empty bodies). 3 parallel task subagents investigated the highest-suspicion candidates (context-token SSOT, Antigravity Gemini invalid selector, Telegram daemon fixes) with a hard evidence-only/no-invented-fixes contract; I personally investigated and closed 2 more (Anthropic OAuth strict-tool-use bug: ruled out, jeo never sends `strict`; compiled-binary models.json crash: ruled out, jeo's catalog is a plain TS array; EPIPE/broken-pipe crash: confirmed and fixed; macOS low-fd-limit warning: confirmed missing and added).
  - src/util/broken-pipe.ts + src/cli.ts: `jeo --help | head` previously dumped a raw EPIPE stack through the process-level uncaughtException net; now exits quietly with 141 (128+SIGPIPE) on both the fatal handler and the dispatch catch block. Reproduced pre-fix (real Bun child process piped into a reader that never reads a byte) and confirmed post-fix.
  - src/util/nofile-limit.ts + src/commands/launch.ts: macOS low `ulimit -n` startup warning (stderr only, JEO_SKIP_NOFILE_CHECK=1 opt-out). Discovered mid-implementation that Bun raises its OWN process's soft RLIMIT_NOFILE to the hard limit at startup — a naive repro (lowering only the shell's soft limit) silently never triggered; fixed the repro (lower both soft AND hard) and corrected the doc comments to describe the true "post-Bun-raise effective limit" semantic once understood, rather than the originally-assumed "pre-raise shell limit" one.
  - src/agent/engine.ts (AgentLoopEvents.onUsage gains optional `lastCall`), src/tui/app.ts, src/tui/components/footer.ts: the TUI footer's context-window percentage was seeded once per turn from a client-side ~4-chars/token estimate and never updated with the real usage every provider adapter already reports — now anchored to the provider's own last-call inputTokens the instant a real response arrives (gjc v0.10.1 "provider-reported usage as SSOT" parity).
  - src/agent/notify/topic-registry.ts (new `wouldRename` peek), src/agent/notify/telegram-daemon.ts: the local TopicRegistry committed a session's new Telegram topic name BEFORE the remote editForumTopic call succeeded, so a transient failure permanently stranded the remote topic at its provisional name (the next identical identity_header silently skipped retrying). Registry commit now happens only after remote confirmation. Mutation-tested: the new regression test was run against the pre-fix source via git stash and genuinely failed as predicted.
  - Ruled out with direct evidence, no action taken: Antigravity gemini-3.1-pro-high invalid selector (jeo independently fixed the same root cause a day before gjc's PR, via a superior BUILTIN_ALIASES redirect in v0.8.3); 8 of gjc's Telegram-daemon fixes (topic-delete races, bot-mention gating, ask-reply ack ordering, /rich-toggle confirmation, control slash commands, forum_topic_edited preservation, ask-selection ack, live-streaming+in-place-editing) are architecturally inapplicable — jeo never deletes topics, has no group-chat model, no `ask` tool, and no durable in-thread config commands.
- Release: CHANGELOG.md [0.8.26] entry (Removed/Fixed/Added/Ruled-out/Verified sections); package.json 0.8.26; bun run changelog:sync mirrored latest 5 entries + 2 new Highlights bullets (EPIPE quiet-exit, macOS fd-limit warning — user-facing reliability/diagnostics; the other 3 fixes are internal correctness/catalog curation, kept CHANGELOG-only per the established convention of not Highlighting every bugfix) into all four READMEs, hand-translated (ko/ja/zh) to match.
- Verified: bun run typecheck clean; full bun test 3075 pass / 0 fail across 304 files (3050 baseline + 25 new: 8 broken-pipe incl. 2 real-child-process E2E tests, 9 nofile-limit incl. a real-CLI E2E test with an isolated no-credential env — corrected mid-session after an advisory caught it silently making a real billed LLM call on every test run, 3 context-usage-ssot, 4 topic-registry wouldRename, 1 telegram-daemon rename-retry integration test). Live end-to-end throughout: haiku-removal fallback pools computed via a real eval harness before writing assertions (not inferred); EPIPE fix reproduced before/after via a real spawned Bun process; nofile-limit warning reproduced against the REAL Bun runtime's documented soft-limit-raise-to-hard-limit behavior (confirmed via web search + empirical test, not assumed) and confirmed stderr-only (never polluting one-shot -p's piped stdout reply); Telegram fix mutation-tested via git stash.

[2026-07-03] Pass — v0.7.32 release: agent-loop guardrail audit + gjc tool-surface parity (ast/lsp/debug/irc/job/goal) + launch.ts slash refactor
- Request: investigate where jeo-code's system prompt/workflow guardrails actually trigger (via parallel subagent audit across engine/loop-guards/task-tool, session/tools, role prompts, and skill workflows), then harden the concrete gaps found; release the result as v0.7.32 (changelog + README latest-5 digest + branch + npm publish), bundling a large concurrent-session feature effort already sitting in the working tree.
- Guardrail audit (4 parallel architect subagents, 90+ findings across engine.ts/loop-guards.ts/task-tool.ts, session.ts/tools.ts/hooks.ts, subagent role prompts, and deep-interview/ralplan/team/ultragoal) surfaced four places where an existing safety mechanism was inert or had a bypassable escape hatch — all four hardened this pass:
  - src/agent/tools.ts: every bash call (incl. the otherwise-unconstrained bundled `executor` role) now runs through a narrow destructive-command denylist (bashDestructiveReason) — rm -rf on root/home, fork bombs, raw disk-device overwrites (dd), raw-device filesystem formats (mkfs). Everyday destructive-but-scoped commands are untouched.
  - src/agent/task-tool.ts: spawn-gate justification check replaced length-only (>=20 chars) with isMeaningfulJustification (rejects repeated-char/token padding, requires >=4 distinct words).
  - src/agent/engine.ts: done-verification pushback's "second done always passes" escape hatch now requires sawActionSincePushback — a done resent unchanged with zero intervening tool call bounces again instead of silently passing.
  - src/agent/tools.ts editTool: a ≔A..B line-range replace with NO anchors and NO prior read this session is now rejected (was completely unverified); SEARCH/REPLACE, anchored edits, and insert/append are unaffected; check runs after the existing out-of-bounds validation.
  - Test fallout from the done-gate/stale-read changes required updating 6 pre-existing tests (done-verification-guard, post-turn-feedback ×2, repeat-bounce-and-trim, tools-fs, edit-freshness) whose mocks encoded the old "immediate 2nd done passes" / "blind unread replace succeeds" assumptions into scenarios with a real intervening action or a prior read.
- Pre-flight safety check before bundling all concurrent work into the release (progress.txt's 0.7.31 entry recorded a prior concurrent session attempting to remove the "Decline to build malware, exploits, or vulnerability-weaponization" WORKING_DISCIPLINE line and invert its lock test): re-verified src/agent/engine.ts:183 and test/prompt-discipline.test.ts's "Phase 1" lock test both still assert the guardrail is PRESENT (not inverted) before committing.
- Bundled a large concurrent-session feature effort found uncommitted in the same working tree (release scope confirmed with the user given its size): gjc tool-surface parity — ast_grep/ast_edit (structural TS/JS search+codemod via the `typescript` compiler API, pure-TS, not the native binary), lsp/lsp_rename (in-process ts.LanguageService definition/references/hover/symbols/diagnostics + cross-file rename, not a real LSP server), debug (Node.js CDP client, node --inspect-brk, Node-only), irc (parent↔detached-subagent live peer messaging over the existing SubagentRegistry.steer() path), job (background shell-process control: start/list/tail/await/cancel), goal (model-settable natural-language stop condition wrapping the existing GoalState); plus a launch.ts slash-command refactor into src/commands/launch/{agents,model,session,system}-slash.ts (~600 net lines out of the monolith).
- Release: CHANGELOG.md [0.7.32] entry (Added/Hardened/Verified); package.json 0.7.32; bun run changelog:sync mirrored latest 5 entries into all four READMEs (0.7.27 aged out).
- Verified: bun run typecheck clean; full bun test 2255 pass / 0 fail across 257 files (includes all new tool suites: ast-grep/ast-edit/ast-match/lsp/lsp-rename/ts-language-service/debug/irc/job/goal-tool + subagent-toolset-gating + the 4 updated guardrail-hardening suites).
[2026-07-03] Pass — v0.7.31 release: deep-interview repeat-question loop guard, gajae tmux sizing parity
- Request: fix the workflow flaw where deep-interview could repeat the same question forever; port gajae-code feature/stability/perf/OS-compat improvements; release 0.7.31 (changelog + README latest-5 digest + branch/merge + npm publish).
- A concurrent session in this same working tree also attempted to remove the "Decline to build malware, exploits, or vulnerability-weaponization" line from WORKING_DISCIPLINE (src/agent/engine.ts) and inverted test/prompt-discipline.test.ts to lock the removal. That change was found uncommitted, reverted, and NOT shipped in 0.7.31 — it is a safety guardrail on a publicly distributed coding agent, not project style; declined regardless of framing. Both files restored to their prior (guardrail-present) state before this release.
- Repeat-question loop guard (deep-interview): normalizeQuestion (case/whitespace/trailing punctuation) + consecutive-repeat tracking mirroring loop-guards repeat_correct/repeat_stop — first repeat gets ONE corrective bounce (model steered to a new gap, user NOT re-asked); a repeat surviving the correction stops the run with a user-visible [LOOP GUARD] message (no-answer-deadend spec constitution #1 honored). Applies to the model's own questions AND the [HOLD] acceptance-criteria followup. Socratic system prompt gains a "never re-ask an answered question" rule. --auto runs that burned 10 identical rounds now stop at 3 calls. Tests: 2 updated (10→3 call expectations) + 2 new (alternating questions never trip; reworded repeats normalize).
- gajae-code fresh clone (0.7.10 HEAD) re-analysis: rate-limit fixes #1369/#1370 already at parity in src/util/retry.ts (0.7.28 pass); ported #1376 managed tmux launch sizing — callerTmuxTerminalSize/tmuxNewSessionSizeArgs (src/commands/launch/tmux.ts) size the detached new-session -x/-y to the caller terminal and re-assert via resize-window pre-attach, ending 80x24 mis-wrapped first frames. +2 unit tests in test/tmux.test.ts.
- Release: CHANGELOG 0.7.31 entry covers the perf pass (rounds 1-3, prior entry below) + repeat-question guard + tmux sizing; package.json 0.7.31; changelog:sync mirrored latest 5 entries into all four READMEs.

[2026-07-02] Pass — Speed & Memory Optimization (Ponytail mode: full)
- Request: Optimize jeo-code speed and memory usage, with three rounds of iterative refactoring, review, and verification.
- Finding: Substantial allocations, O(n²) string scans, and redundant calculations existed in the streaming pipelines, memory injection hot paths, and TUI render loop.
- Round 1 (Streaming & Engine Source Fixes):
  - src/agent/tools.ts: Capped `drainPipe` buffer accumulation at 104KB and emitted only a 16KB rolling tail to `onChunk` (making downstream sanitize/wrap O(L) instead of O(L²)).
  - src/agent/engine.ts: Throttled stream emissions (`onModelStream`/`onReasoningStream`) to ~80ms to prevent rope flattens and TUI re-scans per token, with a guaranteed post-call final flush.
  - src/agent/engine.ts: Precomputed call digests once, eliminating triple re-hashing of write arguments across repeat, cycle, and step-budget novelty checks.
  - src/agent/compaction.ts: Simplified `estimateTokens` by removing redundant conditional branches and grouping ASCII vs non-ASCII additions.
  - src/tui/app.ts: Sanitized only the visible 16KB tail in `onToolProgress` to avoid O(L) scans.
- Round 2 (Memory & AI Layer Fixes):
  - src/agent/memory.ts: Hoisted corpus stats and concept graph builds once per `selectWithinBudget` instead of rebuilding twice on subsets per turn.
  - src/agent/memory-graph.ts: Cached `undirectedAdjacency` lazily on the `ConceptGraph` instance to avoid O(seeds × edges) reconstruction.
  - src/agent/memory.ts: Precomputed lowercased Concept fields via a module-level `WeakMap` cache, eliminating redundant lowercasing and tag array scans per turn.
  - src/agent/memory.ts: Bounded concept stat/read parallelism to 16-file batches in `loadConceptsFromBundle`, preserving speed without unbounded FD/RAM spikes.
  - src/ai/model-manager.ts: Replaced per-chunk Promise + setTimeout/clearTimeout allocation with a local single sliding-deadline watchdog per stream attempt (no internal fields leaked into the exported options type).
  - src/ai/think-tags.ts: Rewrote `partialTail` allocation-free using a simple index-based charCodeAt loop instead of `slice`.
  - src/ai/sse.ts: Rewrote `readLines` line splitting to scan incrementally with `indexOf` instead of split on the full accumulated buffer.
- Round 3 (TUI & Components Fixes):
  - src/tui/components/evolution.ts: Quantized `applyDimensionalGradient` times to 225ms buckets and added a 16-slot gradient memo cache, preventing truecolor escapes from being re-rendered and printed to the terminal on 100% of idle ticks.
  - src/tui/app.ts: Moved `tools.render` below the inline layout early-return, avoiding dead chalk rendering on the default path.
  - src/tui/app.ts: Hoisted `detectColorLevel` once in the class initializer, avoiding EnvLike parsing 2-4× per draw.
  - src/tui/components/width.ts: Added a plain-ASCII fast path and restructured `wrapTextWithAnsi` to avoid `visibleWidth` calls over the remaining text in the loop, bringing the common wrapping path from quadratic O(L²/cols) to linear O(L).
  - src/tui/app.ts: Memoized the Ctrl+O history panel wrapped lines to prevent full history re-wrap on every tick.
- Verified: targeted pass suites all green; full verification pending this entry's final run.


[2026-06-19] Pass — gjc update: round 16 verification-quality inheritance
- Request: "gjc update" — advance the gjc-inheritance marathon (last: cycle 25 / round 15) by re-diffing the CURRENT gjc system prompt (newer than the v0.4.3 the doc analyzed) against jeo.
- Finding: prompt/tool-output/workflow/planning/provider parity is exhausted. Remaining gjc deltas are heavy architecture (async/detached subagents + irc) or already adopted/rejected. The one lightweight gap: gjc's <verification> test-quality contract (jeo's directive only said "run the test", never what a GOOD test is). Also caught a parallel-convention smell — the done self-check sentence was duplicated verbatim in engine.ts (executorSystemPrompt default arg) and launch.ts (interactive prompt).
- src/agent/engine.ts: new single export VERIFICATION_DIRECTIVE = the done self-check + gjc clause ("tests exercise observable behavior, edge values, branch conditions, invariants, error handling — never assert defaults or tautologies"); executorSystemPrompt default arg now references it.
- src/commands/launch.ts: imports + consumes VERIFICATION_DIRECTIVE in place of the hardcoded literal (single source, no drift).
- src/prompts/agents/executor.md: same test-quality constraint added to <constraints>.
- test/prompt-discipline.test.ts: +2 lock tests (constant carries clause + woven into executorSystemPrompt; launch consumes the shared constant, old literal gone).
- Verified: bun run typecheck 0; full bun test 1709 pass / 0 fail (+1 pre-existing tui-app truecolor highlight flake — reproduced identically with changes git-stashed, so unrelated to this cycle).
[2026-06-19] Pass — /login + /provider gjc-parity polish
- Request: improve jeo's /login and /provider to behave like gjc's.
- Found: jeo already had the alias (/login → /provider login), a live-status login picker (account · expiry), and login/add subcommands. The concrete gjc gap was (1) bare /provider only printed static usage text where gjc opens an INTERACTIVE onboarding selector (OAuth-login vs API-compatible endpoint), and (2) no headless-callback hint.
- src/tui/components/provider-picker.ts: added pure builders buildOnboardingChoices() / onboardingPicker() / renderOnboardingPicker() + OnboardingAction type (gjc's provider-onboarding-selector parity). OAuth-login row first (common path), then API-compatible setup.
- src/commands/launch.ts: pickOnboardingAction() closure (mirrors pickCloudProvider's runSelectPicker loop). Bare /provider in a TTY now opens the selector; "oauth-login" routes into the existing login branch, "api-add" prints the add usage, cancel/non-TTY/help fall through to the static readiness panel (scriptable path unchanged). Onboarding usage gained a "Headless OAuth: paste the redirect URL or code…" line (the interactiveOAuthLogin manual-code prompt already exists).
- src/tui/components/slash.ts: /provider + /login descriptions updated to mention the interactive selector and headless paste.
- Tests: test/pickers.test.ts +2 (onboarding order/hints, default selection). Focused: pickers + launch-flag-aliases + oauth + slash + autocomplete + skill-picker all green.
- Verified: bun run typecheck 0; bun test 1630 pass / 0 fail.

[2026-06-10] P2-7 + P2-10 iteration
- P2-7: src/agent/task-tool.ts — fenceSubagentReport() wraps echoed done.reason in <<<subagent-report ... >>> DATA fence; <<< / >>> inside the report neutralized to ‹‹‹ / ›››. Test added in test/task-tool.test.ts.
- P2-10: PlanSchema/StepSchema/normalizePlanShape/parseYaml moved from src/commands/team.ts to new src/agent/plan.ts; team.ts, ralplan.ts, test/team-schema.test.ts import the shared module. team.ts no longer imports zod.
- Also this session (pre-ralph): catalog substring offsets, namespaced alias owners, reasoningEffort enum guard, gemini URL-encoding, 7 new tests, TUI glyph flake fix.
- Verified: build exit 0; 705 pass / 0 fail.
[2026-06-10] Pass 874 — 429 resilience + box-only input
- retry: escalating 429 floor (2s→30s cap), default budget 6 attempts (~60s total), onRetry delay surfaced end-to-end as AgentLoopEvents.onNotice (TUI/plain/team/subagent).
- engine: terminal LLM error reported once (doneReason only); onError removed.
- launch: boxed input footer adaptive 5–12 rows (short terminals keep the box), empty readline prompt in box mode — raw `jeo>` input never visible.
- Verified: tsc 0; build ok; bun test 724 pass / 0 fail. Committed eb37122.
[2026-06-10] Pass 878 — Google OAuth bundled client secret
- google.ts: requireClientSecret() (env-only, broke login at token exchange) → googleClientSecret(): GEMINI_OAUTH_CLIENT_SECRET override → bundled gemini-cli default (b64). Test in oauth.test.ts; AGENTS.md note corrected.
- Verified: typecheck 0; bun test 732 pass / 0 fail.
[2026-06-10] Pass 879 — gjc-parity + Gemini thinking-budget + live model verification
- SKILL.md embeds (src/prompts/skills), hierarchical AGENTS.md context walk, OAuth refresh file-lock + atomic config save, streamMaxRetries consumed, picker company branding.
- gemini.ts: geminiThinkingBudget() — flash default 0, pro floor 128, pre-2.5 omitted, effort-mapped + clamped; empty MAX_TOKENS now errors (fixed live empty-reply on gemini-flash-latest).
- scripts/verify-models.ts: live per-provider recent-model verification → 0 hard failures (rate-limited models distinguished).
- Verified: typecheck 0; bun test 743 pass / 0 fail; build ok.
[2026-06-10] Ratchet loop — gjc-parity wave 1 (parallel subagents)
- Baseline: typecheck 0, 758 pass. gjc v0.4.3 surface diffed against jeo → gap list.
- Wave 1 (4 parallel executors): jeo state (read/write/clear/handoff over .jeo/state), jeo session (tmux list/attach/rm with injectable runTmux), launch -p/--print + -c/--continue + --append-system-prompt (@file), jeo update (--check/--install/--json/--strict, injectable deps).
- Registry wired in runner.ts (state/session/update). 1 keep/revert event: update.ts string|null narrowing fixed at gate.
- Gate: typecheck 0; 789 pass / 0 fail (+31). Live smoke: state write/handoff/read + unknown-skill exit 1; session list; update --json all verified.
[2026-06-10] Ratchet loop — wave 2 + OAuth operability + UX fixes
- Wave 2 (3 parallel executors): launch toggles (--no-skills/--skills globs/--no-tools/--tools/--system-prompt + buildToolProtocol/filterToolMap), jeo skills list|read --json, jeo export --html (renderSessionHtml).
- ROOT CAUSE found by live verification: `jeo chat --model X` swallowed the flag into the message → always chatted with the DEFAULT model. This masked anthropic/antigravity selection entirely ("OAuth 모델이 동작 안 함"). Fixed via parseChatArgs + test; claude-haiku-4-5 OAuth now answers live (12in/4out tokens).
- Engine: prose-salvage fix — pure-prose LLM replies become done.reason instead of bounced corrections (killed the "죄송합니다… JSON tool call" apology-as-reply bug); correction text now forbids apologies + offers done-resend; MAX_PARSE_BOUNCES=2 then salvage. New onUsage event (cumulative per-call).
- TUI: formatDuration/formatTokenCount/formatUsage (duration.ts) — "took 3 steps in 1m 45s · 14.2k in / 1.1k out tokens" summary; per-turn usage tracked via onUsage.
- Gate: typecheck 0; 831 pass / 0 fail (+42). Wave 3 in flight: plain-stream live status, gradient status animation, gemini-cli credential import (antigravity unlock).
[2026-06-10] Mock/fallback purge + TUI insight rows (pass 882)
- model-discovery: ANTIGRAVITY_MODELS static catalog fallback REMOVED. Antigravity lists models from the live Cloud Code Assist `v1internal:fetchAvailableModels` (POST) — gjc parity. Response decoded from real payload: map KEY is the callable id (entry.model is an internal MODEL_PLACEHOLDER_* enum); `agentModelSorts` groups are the API's positive agent set (preferred); non-chat role lists (tab/image/transcription/commit/mquery/deprecated-as-object) excluded data-driven. catalogOr now falls back ONLY for OpenAI/Codex.
- TUI: status split into [STEP] (metrics: step n/m · meter · elapsed · avg) / [STATUS] (live insight: current file, retry backoff) / [TOOL] (forge). Retry notices pinned to [STATUS] only — never appended to the stream log (no more repeated "rate limited" rows).
- Live proof: `jeo models` → antigravity (oauth): 8 agent models (exact product picker set); `jeo chat --model antigravity/gemini-3-flash` → "OK" (6 in / 95 out tokens) on dedicated Antigravity OAuth.
- Gate: typecheck 0; 864 pass / 0 fail. Keep/revert events this pass: deprecatedModelIds object-shape spread crash caught by live run and fixed + regression test.
[2026-06-10] Pass 883 — $skill 호출 + HUD phase flow + TUI 연속입력/스타일
- skills: `$스킬명 [intent]` 직접 호출(첫 토큰 정확 일치만 발동; $HOME 등은 모델로 통과) + Tab 자동완성($sp → $spec-kit) + `jeo skills`에 디스커버리 경로/호출법 노출.
- TUI: finish()가 alt-screen 복귀 후 clear-to-EOL + clear-below로 잔존 행 제거 — 요약 겹침/깨진 이중 입력박스 해소, 연속 요청 입력 구조 검증(tmux 라이브 2턴). [STEP] 행에 라이브 토큰 사용량(`4.1k in / 42 out`).
- HUD: thinking→planning→executing→reporting→done phase 행(완료 체크/현재 강조/미래 dim) + [STATUS] 그룹 위아래 간격; 턴 종료시 `[DONE] thinking → … · steps · 시간 · 토큰` 정리 보고.
- 스텝/Plan 체크리스트: 완료=체크+취소선+dim, 실패=빨강, 진행=시안 볼드 (color:false 경로 바이트 동일 유지).
- Gate: typecheck 0; 883→885 pass / 0 fail. 라이브: tmux 2연속 턴 + phase report 클린 출력 확인.
[2026-06-10] Pass 881 — deep-dive gjc parity execution
- Native workflow routing in launch: `/skill deep-interview|ralplan|team|ultragoal` now hits real engines; bundled workflow docs live in `src/prompts/skills/*/SKILL.md`.
- Hook surface: opt-in `hooks.enabled` + safe pre-tool/post-turn execution via `Bun.spawn`; AGENTS hierarchy walk + configured-skill prompt advertising landed.
- Provider/model/memory: OpenAI OAuth non-Codex fail-fast, Antigravity provider/catalog wiring, company labels in `/model`·`/provider`·`/config`, token-based compaction + session compaction markers + team compaction.
- TUI/UX: footer `ctx NN%`, red-claw/blue-crab themes, in-flight abort harness (Ctrl-C cancel, ESC cancel in live TUI, second Ctrl-C hard exit).
- Verified: typecheck 0; bun test 831 pass / 0 fail; build ok; live smoke with `claude-haiku-4-5` => smoke ok.
[2026-06-10] Pass 882 — Antigravity OAuth parity + role-model UX cleanup
- Antigravity: shared Cloud Code Assist project discovery (`loadCodeAssist`/`onboardUser`), dedicated `jeo auth login antigravity`, lazy projectId self-heal/persist, improved 403 guidance.
- Command surface: removed `/subagent` and `/subagents`; keep role config in `/agents` and new `/model subagent <role> [model|#N]`.
- Verified: typecheck 0; focused 96 pass / 0 fail; full bun test 858 pass / 0 fail; build ok.
[2026-06-10] Pass 883 — antigravity selectable in /model + role-pin verified
- /model·/provider·/agents·/model subagent: antigravity(OAuth 보유 시) 선택 거부 → 경고+허용으로 전환; 다른 provider는 기존 거부 유지.
- REPL 검증 테스트 추가(gemini-fallback config에서 /model 선택 + /model subagent 핀 영속); TUI stage 테스트 뷰포트 200x40 고정으로 플레이크 제거.
- Verified: typecheck 0; bun test 859 pass / 0 fail; build ok.
[2026-06-10] Pass 884 — mouse-wheel scroll TUI corruption fix
- enterAltScreen: ?1049h+?1007l (alt screen + alternate-scroll OFF) / leaveAltScreen: ?1007h+?1049l — 휠이 화살표 시퀀스로 주입돼 다음 프롬프트를 깨뜨리던 문제 제거(tmux 동일 적용).
- REPL은 매 프롬프트 전 pending tty input drain + readline 버퍼의 escape/arrow 노이즈 정리.
- Verified: typecheck 0; bun test 859 pass / 0 fail; build ok.
[2026-06-10] Pass 885 — TUI 자동 복구 (수동 키 불필요)
- 주기 리싱크: 라이브 턴 틱 25회(~3s)마다 차등 베이스라인 리셋 → 모든 라인 재기록으로 화면 오염 자동 치유.
- resize 자동 리페인트(start 등록/finish 해제), 휠 노이즈 감지 시 즉시 repaint(onNoise), raw-mode Ctrl-C(\u0003) 데이터도 abort 경로로 정합.
- Verified: typecheck 0; bun test 863 pass / 0 fail; build ok.
[2026-06-10] Pass 886 — TUI 에코 억제 및 인풋 박스 중첩 버그 수정
- runSelectPicker: 진입 시 rl.pause() + raw mode 강제 적용 / leave 시 rl.resume() 복원. 피커 작동 중 readline 입력 수신 및 에코 간섭 완벽 격리.
- GATED_OUTPUT_METHODS: _write, _writev 추가로 Bun 저수준 스트림 우회 차단.
- drawFooter: 시작/끝에 toColumn(1) 강제하여 한글 CJK 너비 오판으로 인한 커서 오프셋 밀림 차단.
- Verified: typecheck 0; bun test 871 pass / 0 fail; build ok.
[2026-06-10] Pass 887 — 실제 PTY(tmux) 동작검증 + 피커 버그 2건 추가 수정
- 검증법: 100x30 tmux PTY에서 jeo launch → /model 피커 → 방향키+한글필터(너의페르소나) → capture-pane 단언.
- 발견/수정: ① rl.pause()는 피커 keypress까지 굶겨 행 위험 → 출력 게이트(previewArmed||pickerActive) 방식으로 교체, ② repaint/clear off-by-one(cursorUp(rendered)→rendered-1)로 매 키입력마다 힌트행 잔상 누적되던 실버그 수정 + 기존 행 이동은 \n 대신 CUD, ③ 피커 종료 시 rl.line/cursor 클리어(다음 프롬프트 보이지 않는 필터텍스트 누출 차단).
- 라이브 결과: 필터행 1개/잔상 0, BSpace 후 목록 복귀(행 없음), ESC 후 박스 1개 복원, /exit 정상.
- Verified: typecheck 0; bun test 871 pass / 0 fail; build ok.
2026-06-10: Executed deepinit and code-refactoring for V2. Extracted output-util and tool-registry. Lean engine.ts loop finalized. Updated AGENTS.md files.
[2026-06-11] Pass 889 — gjc-forge TUI parity wave (4 parallel executors) + inline-scrollback QA closed
- Inline tmux scrollback (888b 위에 정합): QA 하니스 캡처 버그 2건 수정 — copy-mode 뷰는 scroll_position 기반 -S/-E로 캡처, 깨끗해진 히스토리는 history-top까지 스크롤. logs/qa-inline-scrollback/runner.sh 6/6 PASSED (happy/copy-mode/no-alt/resize/burst/dedupe, tmux 3.6a).
- Welcome forge box (src/tui/components/welcome.ts): gjc 'Gajae forge' 구조 이식 — 타이틀 임베드 보더, 좌측(브랜드+태그라인+진화 아트+model/provider 필), 우측(Flow keys/Workspace/Session trail), <64col 단일 컬럼, <30col 한 줄 폴백, basename dedupe. launch 인터랙티브 배너의 평문 === 3줄 대체.
- Update Available 박스: src/util/update-check.ts(npm latest, 2.5s 타임아웃, never-throws) + src/tui/components/update-box.ts(warning 보더). 배너 직후 1.2s race로만 출력 — 라이브 프레임 오염 금지.
- Footer: cwd?(~축약, 32자 middle-truncate)+branch?(git symbolic-ref — tmux 세션 네이밍과 동일 호출) 세그먼트, ctx 세그먼트 gjc 포맷 'ctx {pct}%/{200k}'(k/M 포맷, ≥60% yellow ≥85% red, (auto) 표시). launch.ts:936 setContextUsage 라이브 공급 확인.
- Verified: typecheck 0; bun test 920 pass / 0 fail (신규 tui-welcome 11 + update-box + footer 케이스 포함); tmux QA 6/6.
[2026-06-11] Pass 890 — scrollback goal closure: README documents pass-889 surface (glyph-first ✔/✗ ledger, [STEP] ⤴ tok/s, /exit resume pointer, copy-mode wheel note); QA harness re-run on current build 6/6 PASSED (tmux 3.6a); typecheck 0, 926 pass / 0 fail; pushed.
[2026-06-11] Pass 891 — shadcn-inspired stage-grouped card layout (gjc reference)
- Captured live gjc (logs/gjc-sample) + current jeo (logs/jeo-tui-study via subagent) TUIs in tmux; found 3 real flaws: giant outer box with (a) ~20-col dead right margin on nested forge boxes (capped 96 inside ~116), (b) 7+ blank filler rows of dead space between content and HUD, (c) no section rhythm/stage grouping.
- New src/tui/components/section.ts: SECTION_GAP + sectionLabel (muted '── Output ──' card-header divider, gjc '├─ Output ─┤' style) + stackSections (consistent blank-line rhythm, drop-empties). Unit-tested (test/section.test.ts, 6 cases).
- app.ts renderForge: in-frame forge boxes now fill width (cap 120 not 96) — dead right margin gone. draw() fit branch reassembled shadcn header/spacer/content+footer: art banner top → breathing filler → stage cards (Plan / ── Activity ── / ── Output ──) hugging the bottom status panel → ├──┤ divider → HUD. Budget-accounted so total never exceeds rows.
- Verified live (logs/jeo-redesign/layout-driver.ts in tmux): alternate_on=0, stage cards render, forge boxes full-width, dead filler eliminated, glyph-first ✔ ledger still flushed into scrollback above the frame (mouse-wheel contract intact).
- Verified: typecheck 0; bun test 932 pass / 0 fail (+6); inline-scrollback QA 6/6 PASS (tmux 3.6a).
[2026-06-11] Pass 892 — input-box caret + gjc tree ledger
- input-box.ts: gjc-style '> ' prompt marker leads the boxed input (red accent, dim placeholder); new renderInputFrame() maps the readline caret (rl.cursor) to its terminal cell across display-width wrapping/CJK/truncation.
- launch.ts: drawFooter parks the REAL terminal cursor at the caret cell (right after '>') instead of the reservation top — blinking cursor marks the insertion start point and visibly follows arrow keys/Home/End; caret cell joins the repaint key so pure arrow moves repaint; disarm/draw hop back from the parked row.
- app.ts: ledgerTree() — find/search/ls results flush gjc-style ' · N files/matches' count suffix + up to 6 dim '├─' child rows + '└─ … N more' into scrollback (wheel-history reads like gjc's '✓ Find: pattern  39 files' block).
- Aligned stale stream-events assertions to the concurrent session's new 'Read :' title format (tolerant regexes accepting both).
- Live tmux proof: cursor_x=3 at start point after '>', =8 after 'hello', =6 after 2x Left, =3 after Ctrl-A. Gate: typecheck 0; bun test 981 pass / 0 fail.
[2026-06-11] Pass 896 — $ skill popup + ESC/^C input clear
- slash.ts: formatSlashPreview/slashPreviewMatches handle a leading '$' — live skill popup ($name [intent] + summary, shared highlight/scroll-window renderer with '/'), arrow-key nav + Enter selection wired in launch (pendingSelection now applies to $ prefixes).
- launch.ts: clearTypedInput() — ESC (lone-ESC decodes with meta set; accepted) and Ctrl+C (rl SIGINT listener) wipe the typed line + pending clipboard images and redraw the placeholder box; empty-line ^C shows '^C — nothing to clear · /exit to quit' (generic keypress redraw skips ^C so the hint survives). Cmd+C never reaches terminal apps (OS copy) — meta-mapped variants accepted.
- Live tmux proof: ESC/^C → placeholder + caret x=3; empty ^C hint row; '$' popup lists 4 workflow skills with summaries.
- Gate: typecheck 0; bun test 997 pass / 0 fail (slash $-popup +2 tests).
[2026-06-11] Pass 897 — /history in-app turn-history viewer
- src/tui/components/transcript.ts: formatTranscript() folds engine history (prompts / JSON tool calls / 'Tool [x] result' feedback / bounces) into a gjc-style transcript — 'user ▸' blocks, compact ✔/✗ tool ledger lines (verdict from the paired result message, title via summarizeForgeInvocation), 'jeo ◂' reply blocks; maxTurns slices whole prompt-anchored exchanges with an '… N earlier turn(s) hidden' marker; bodies clipped with '(+N more lines)'.
- launch.ts: '/history [n|all]' (default last 5) re-prints the worked history into scrollback so past work can be reviewed by scrolling up even where terminal scrollback can't reach (tmux w/o mouse, cleared screens, resumed sessions); registered in the '/' popup + /help (group session).
- Restored test/review-fixes.test.ts FIX 3 block (concurrent-session paste corruption left an unbalanced brace breaking the whole suite file).
- Live tmux proof: /history with no turns → '(no worked history yet)'; after a turn → header + user ▸ + ✔/✗ ledger + jeo ◂. Gate: typecheck 0; bun test 1004 pass / 0 fail (+4 transcript tests).
[2026-06-11] Pass 898 — symbolic Primordial Cell (evolution forge cell) after assets/character.png
- ascii-art.ts: redesigned stage-0 "Primordial Cell" art from the flat `.---.`/`o o` blob into the jeo-code mascot — antennae with glowing tips (○↔◉), a round box-drawing membrane (╭───╮/╰───╯), big kawaii eyes (◕↔◔), an inner DNA helix core (§↔≋), a smile (‿), and crab-like legs. Two "breathing" frames: blink + helix pulse + leg swing.
- Width-safe by construction: every glyph is display-width 1 (box-drawing + ambiguous-width geometrics the TUI already uses), so renderAsciiArt's length-based padding and app.ts/welcome.ts's visibleWidth re-centering stay in lockstep — no smearing/misalignment. stageWidth/stageHeight unchanged (still 20×8).
- Restored graceful degradation: AsciiStage.asciiArt/asciiFrames + renderAsciiArt `unicode` option (wired through welcome.ts/app.ts/evolve.ts). unicode:false terminals get a tofu-free ASCII cell (o/O eyes, 8/% helix, \_/ smile) instead of box-drawing tofu.
- Verified: typecheck 0; bun test 1021 pass / 0 fail (+2 cell-fallback/width tests); live 118x42 tmux launch shows the cell centered in the welcome banner with every border aligned (no overlap/smearing) under unicode and ASCII paths.
[2026-06-11] Pass — dynamic step budget (no 100-step stop) + in-tmux wheel scroll
- launch: --max-steps default 100 → 0 (= dynamic). Engine: non-finite/<=0 maxSteps selects dynamicStepBudgetConfig — JEO_STEP_BASE 24 rolling base, UNLIMITED extensions while the recent tool window shows novel progress, DYNAMIC_HARD_CAP 600 as a termination guarantee only; a stalled window declines and the loop consolidates the wrap-up (취합) instead of dying at a count. Explicit --max-steps keeps the bounded flow; subagent {maxExtensions:0} contracts unchanged.
- TUI/stream `step N/M`: M seeds from initialDynamicStepLimit() and grows live via onBudget; tmuxRuntimeSuffix stale `!==25` check fixed (only explicit caps name the session).
- tmux scroll: `jeo --tmux` INSIDE an existing tmux session now best-effort enables session-scoped `mouse on` (shouldEnableCurrentTmuxMouse — JEO_TMUX_MOUSE=0 opts out, JEO_TMUX_LAUNCHED=1 skips) so wheel-up reaches the inline-frame scrollback ledger.
- Repaired concurrent rebrand breakage at gate: engine.ts missing while-loop brace (TS1005), stale pre-rename test expectations (tmux names, resume hint, repeat-guard batch semantics).
- Verified: typecheck 0; step-budget/launch-flags/tmux/engine suites 50 pass / 0 fail (+9 new tests). Full-suite residue is the concurrent jeo-rebrand workstream (session/transcript/update surfaces), churning live.
[2026-06-11] Infinite-loop fix — dynamic step budget termination guarantee
- Root cause: dynamic budget (maxSteps 0 default) shipped with hardCap/maxExtensions = Infinity while the progress heuristic was gameable — (a) batch success scored as some(ok): 5 failing edits + 1 trivial read = "progress" forever; (b) A↔B signature cycling passed distinct>=2 + ok-ratio; (c) repeat guard moved AFTER execution (3rd identical mutating call ran); (d) parseFailures never reset (cumulative, not consecutive).
- step-budget: novelty rule (each extension requires >=1 never-seen signature since the last grant; cycling declines with "no novel tool calls") + DYNAMIC_HARD_CAP=600 finite default (env JEO_STEP_HARD_CAP still overrides) — termination now guaranteed, pathological spins consolidate a wrap-up instead of running forever.
- engine: per-CALL budget window recording (batch of mostly-failing calls scores honestly), repeat guard restored to BEFORE execution, parseFailures resets on a successful parse; engine-multitool >6-truncation test deflaked (parallel read group has no order contract).
- Verified: typecheck 0; bun test 1049 pass / 0 fail.
[2026-06-11] Pass — jeo rebrand + engine multi-tool batching + DNA Claw forge + stream/leak hardening
- ROOT CAUSE (jeo-vs-gjc latency, same model): one JSON tool call per LLM round trip. engine.ts now accepts {"tools":[…]} batches (≤6/step, legacy single-tool unchanged): consecutive read-only calls (read/find/search/ls) dispatch via Promise.all, mutating tools are barriers, batched `done` is rejected with guidance, ONE combined result message per step preserves prompt-cache prefix; budget window scored per CALL. TOOL_PROTOCOL/READONLY advertise batching. test/engine-multitool.test.ts (6 cases, extension-flow pinned via maxExtensions:0).
- Canonical jeo rebrand: bin `jeo`, build dist/jeo, all user-visible strings/prompts (`jeo>`, `jeo ·`, help, errors, SKILL.md docs, READMEs ×4 + rename note), installer/uninstaller/smoke. Env: reads use JEO_* (incl. step-budget envNum/envSet, themes, storage); on-disk ~/.jeo & .jeo state unchanged; tmux session prefix jeo-.
- Forge identity → single DNA Claw symbol: compact (ascii-art.ts DNA_CLAW_ART) for the in-turn header with quantized-phase O(1) render cache, GRAND 12-line variant for the welcome box; flowing 3-stop gradient (green→cyan→violet) animated by tick phase; ASCII fallbacks tofu-free. Welcome rewritten to the gjc hero layout (user screenshot): `─ jeo v… · JEO forge ─` title border, single centered column (Jeo forge / evolve · act · prove / grand claw / model+provider pills, width-truncated), two-column Flow-keys/Workspace/Session-trail panel REMOVED.
- Streaming → history+activity: provider stream preserved end-to-end (all 5 adapters); prose-streaming models (no `reasoning` field) now also flush one dim `jeo · …` thinking line into the scrollback ledger (flushedActivity latch, no dupes); live activity row unchanged.
- Memory-leak guarantees (no cumulative growth): finish() idempotent; resize listener + 120ms timer removed on EVERY end path (success/error/hard-exit); launch turn wrapped try/finally; bounded structures proven by test/leak-guards.test.ts (resize-listener baseline across ≥20 turns incl. error+double finish, timer silence after finish, StreamRegion cap 500, ToolList front-trim, forgeSummaries cap 8).
- Verified: tsc 0; bun test 1049 pass / 0 fail (133 files); smoke `bun src/cli.ts --version` → `jeo v0.1.0`, --help fully jeo-branded.
[2026-06-11] Pass — two-tone box depth everywhere + MRU defaults documented + README/push
- Box depth & contrast (사용자 요청 "박스 깊이감+색상대조"): themes.ts accentShadowPaint() (dimmed accent) paired with accentPaint(); applied lit-top/left vs shaded-bottom/right two-tone to EVERY panel — welcome JEO forge box (title border bold for contrast, theme-accent edges via launch.ts), forge cards (formatForgeBox paintShadow incl. separator/divider right edges), live status box (boxBlock paintShadow), the outer alt-screen frame (app.ts), and the input box (theme-matched accentShadow instead of the fixed blue.dim default). color:false paths byte-identical (tests unaffected).
- MRU defaults (이미 구현 확인 + 문서화): /model · /provider picks persist via rememberModelPatch → defaultModel = newest pick for EVERY session, recentModels = newest-first rotation in ~/.jeo/config.json. README ×4 now documents it alongside the new panel-depth behavior.
- Tests: welcome + forge two-tone depth regression cases with injected painters (lit/shadow markers); color:false byte-stability case.
- Verified: tsc 0; bun test 1056 pass / 0 fail (135 files).
[2026-06-11] Pass — `jeo` 미동작 수정 (stale registry install)
- 원인: npm 레지스트리의 jeo-code@0.1.0은 rename 이전 스냅샷(bin에 jeo만) — `bun install -g jeo-code`가 jeo shim을 만들지 못함.
- package.json 0.1.0 → 0.1.1 (동일 버전 재퍼블리시 불가); cli.ts VERSION 하드코딩 제거 → pkg.version 단일 소스(update 비교 드리프트 차단).
- 로컬 복구: stale 글로벌 제거(bun remove -g, DependencyLoop 해소) → 로컬 타볼 글로벌 설치(jeo+jeo shim) → ~/.local/bin/jeo 링크(PATH 우선순위) → `jeo --version` = jeo v0.1.1 확인.
- 레지스트리 반영은 NPM_TOKEN 보유 CI(npm-publish.yml: release publish 또는 workflow_dispatch) 필요 — 퍼블리시 전까지 타 머신의 registry 설치는 여전히 0.1.0.
- Verified: typecheck 0; install-script + update-command 22 pass / 0 fail.
[2026-06-11] Infinite-loop fix round 2 — live hang vectors (REPL EOF + OAuth lock)
- launch REPL: under Bun a pending rl.question NEVER settles after stdin EOF (Ctrl-D / exhausted pipe) — the while(true) prompt loop hung forever. promptInput() now queues orphan 'line' events (Bun drops piped lines arriving between prompts) and races question vs 'close' → EOF behaves like /exit. Applied to the main prompt, the /provider login picker, and workflow io.input. Stub-safe (on + one-shot guard, not once — launch tests stub readline).
- auth/storage acquireLock: timeoutMs was only the STALENESS threshold; the 50ms retry loop itself was unbounded — a wedged/churning lock (concurrent sessions share ~/.jeo) froze mid-turn OAuth refresh forever. Now bounded at max(2x timeout, 1s): stale removal first, then a one-shot steal, then a clear error.
- New tests: test/launch-repl-eof.test.ts (spawns real CLI, immediate-EOF + piped-command-then-EOF must exit, not timeout), test/oauth-lock.test.ts (perpetually-refreshed lock acquires <3s via steal; stale dead-holder lock removed promptly).
- Verified: typecheck 0; bun test 1056 pass / 0 fail.
[2026-06-11] Pass — DNA Claw twist animation + identity-cached token estimates (ralph ratchet)
- Forge identity ("애니메이션+그라데이션 멋있게"): compact DNA Claw gains 3 helix TWIST frames (claw silhouette fixed, lattice rotates; frame 0 === static art) layered with the flowing gradient — app.ts alt-screen header re-wired to renderDnaClaw (a concurrent pass had reverted it to the 5-stage art) with one int cache key folding twist(3) × gradient-phase(20); single-slot O(1) cache, evolutionTrack/transition lines kept.
- Perf+memory: estimateMessageTokens now WeakMap-cached by message OBJECT IDENTITY (engine/compaction replace, never mutate) — per-turn historyTokens context meter drops from O(total chars) to O(new messages) on long sessions, and a WeakMap structurally CANNOT accumulate (entries die with their messages).
- Repair: restored 4 test() headers in test/tui-app.test.ts that a concurrent-edit race had dropped from the previous commit (orphaned bodies → parse error on pushed main); reconstructed from ec5ce05 with jeo-era expectations and reverted a stray assertion-comment-out (test weakening) — pushed as 0f67543.
- Tests: test/dna-claw-anim.test.ts — frame dimensions/wrap, twist actually differs, gradient animates at TrueColor while plain output is phase-stable, grand hero stays static, token-cache identity semantics.
- Verified: tsc 0; bun test 1061 pass / 0 fail (136 files); launch-repl-eof 20s timeout confirmed as suite-load flake (passes isolated).
[2026-06-11] Pass — one-shot piped slash commands handled without a model call (fix flaky launch-repl-eof)
- Bug: `echo "/clear" | jeo` (and any piped/`-p` slash command) ran in ONE-SHOT mode (!stdin.isTTY => isOneShot), where the literal "/clear" was forwarded to the LLM as a prompt — a slow, flaky, semantically-wrong model round-trip. test/launch-repl-eof.test.ts "drains piped commands then exits at EOF" intermittently TIMED OUT (20s) because that round-trip exceeded the budget; the test's own comment ("/clear is handled synchronously without a model call") described behavior the code did not yet have.
- Fix: src/commands/launch.ts one-shot block now short-circuits control slash commands BEFORE skill/turn dispatch: /exit·/quit => clean exit; /clear·/new·/drop => reset history to the system prompt (+ fresh session file when persisting) and print "(history cleared)"; / · /? · /help => print the slash command list. None hit the model.
- Test: strengthened test/launch-repl-eof.test.ts — captures stdout and asserts /clear prints "(history cleared)" and emits NO `[step ` lines (proves no model call), plus the not-timeout exit. Runs ~1s instead of timing out.
- Gate: tsc 0; bun test 1063 pass / 0 fail (was 1060/1 with the flaky timeout).[2026-06-11] Pass — subagent role targeting in the model picker + /agents interactive editor + welcome sweep
- gjc parity (사용자 요청): an INTERACTIVE model pick (`/provider <name>` arrows+Enter) now asks "Apply <model> to" — default(every session) or one subagent role (executor/planner/architect/critic); each option's hint shows that target's CURRENT model, so the picker doubles as a view-and-change panel. ESC/non-TTY/explicit args keep legacy default-apply. Role choice persists via withSubagentSetting (subagents map), never touching defaultModel/MRU.
- /agents (bare, TTY): interactive editor — role picker (hint: current model · steps · "(default)" tag) → action picker (change model via the live model picker / reset to defaults) → saveConfigPatch. Text subcommands unchanged for scripts.
- subagents.ts: applyTargetChoices() pure helper (testable); launch.ts: generic pickFromOptions arrows+Enter option picker reusing runSelectPicker.
- Welcome sweep (전 턴 검증 갭 마감): playWelcomeSweep() — DNA Claw gradient sweeps one full palette cycle in-place (~0.5s, cursor-up rewrites, per-line EL), FINAL frame byte-identical to the static banner; launch gates on TTY+truecolor, JEO_NO_WELCOME_ANIM=1 opts out. Verified earlier via logs/claw-anim-verify.ts: alt-screen twist frames change glyphs across ticks; default inline mode now gets its animation at launch.
- Tests: subagent-apply-target.test.ts (choices order/hints/change-reflection; sweep frame count, cursor-up height, final-frame parity).
- Verified: tsc 0; bun test 1064 pass / 0 fail (137 files).
[2026-06-11] jeo-ref layout pass — 행간/박스간격 readability (reference: /tmp/jeo-ref.png)
- forge card anatomy: title rides ON the top border (`╭── ✓ Bash ──╮`, was title row + separator = 2 chrome rows), 1-col left gutter for content (`│ $ cmd`), labeled `── Output ──` divider kept. forge-status tests re-pinned.
- ledger rhythm: appendLedger(text, kind) — blank spacer row between DIFFERENT groups (reasoning ↔ ✓-tool lines ↔ cards ↔ notices ↔ subagent) and around every card; same-kind lines stay adjacent. Reset per turn.
- spacing: blank row between model/status bar and the input box (idle footer; MIN_PREVIEW_ROWS 6→7, caret/budget shifted), spacer before the final `jeo>` reply (inline), gap between stream and forge section (plain summary).
- RENDERER ROOT-CAUSE FIX: insertAbove deferred stale-row clearing to the next render via coverRows; at the bottom margin that walk cursor-downed past the margin where clamping desynced the row bookkeeping — every subsequent 120ms frame painted one row higher, devouring the flushed card above (truncated-card corruption, reproduced deterministically in tmux). insertAbove now EL-clears the old frame's uncovered rows EAGERLY (geometry provably on-screen) and hops back to the anchor. tui-renderer test re-pinned + margin-desync regression.
- Verified: typecheck 0; renderer/app/forge/launch suites green; tmux live smoke (gemini-2.5-flash): grouped ✓ lines → gap → intact titled card → gap → reply → bar → spacer → input box, full pane scroll pressure, no corruption. Full suite 1052 pass / 16 fail — all 16 are the concurrent slash.ts palette rewrite mid-flight (commands removed from SLASH_COMMAND_DETAILS while its tests still pin them; file untouched by this pass).
[2026-06-11] Pass — slash palette reconciliation (input-box budget bug + restored commands) atop the inline-corruption/tmux fixes
- Context: the concurrent redesign added gjc-style fuzzy subsequence matching + ▸ preview rows but left 15 tests red, dropped 17 still-working commands from the palette (/thinking /theme /view /diff /find /search /sessions /roles /login /logout /usage /context /tools /hotkeys /settings /evolve /quit), and broke renderPreviewRows' row-budget contract (markers appended BEYOND `max` → preview could overflow the reserved footer rows and shift the input box/caret — the same "입력창 깨짐" class the renderer.insertAbove fix had just killed).
- Fix: hard budget contract restored (overflow spends ≤2 of `max` slots on ↑/↓ markers, output NEVER exceeds `max`); all working commands restored to SLASH_COMMAND_DETAILS with the new groups/usages (incl. /agents interactive-editor usage); new /drop /rename /retry /btw /dump entries kept (handlers verified in launch.ts).
- Tests rewritten to the reconciled contract: prefix-first + fuzzy-trail ordering (slice asserts), ▸ marker, /expt→/export fuzzy case, budget ≤ max re-verified.
- Verified: tsc 0; bun test 1072 pass / 0 fail (137 files) — includes the concurrent pass's renderer insertAbove geometry fix, interactiveTurnActive input gating, jeo-ref forge cards, and tmux `=name:` profile.
[2026-06-11] Pass — Edit card diff view (history ledger gap closure)
- forge.ts editBlockDiffLines(): the Edit forge card now renders the editBlock as a gjc-style diff — SEARCH/REPLACE hunks become `- old` / `+ new` rows (capped, secret-redacted) closed by a `~N hunk(s) · +A −R line(s)` Summary divider; `≔` directives show the directive head + payload as `+` rows with a `+A line(s)` summary. Unknown/malformed formats fall back to the raw preview (never throw). Plain +/- prefixes — ANSI-free in every color mode. Mirrors agent/tools.ts parse rules locally so the TUI layer does not import the agent layer.
- Closes the last gap of the history-ledger request: Task / bash / status gradient / jeo reason / Write(path+lines+summary) / Search tree / Edit(diff lines) / Read paths / hud / conclude all render typed ledger entries now.
- Verified: tsc 0; bun test 1085 pass / 0 fail (139 files).
[2026-06-11] Pass — seamless looping welcome sweep + jeoEnv codemod (refactor) + MRU verified
- Forge 애니메이션 "끊김 없는 반복": playWelcomeSweep v2 — (a) 사이클 루프(기본 2, JEO_WELCOME_ANIM_CYCLES 1-10): phase가 사이클 경계에서 정확히 래핑하고 프레임 딜레이가 일정해 사이클 간 멈춤/색 점프 없이 이어짐, (b) 매 리페인트를 DECSET 2026 BSU/ESU로 원자화 — 느린 터미널에서도 티어링/플리커 없음(= 끊김 없음), (c) 최종 프레임은 phase 0으로 정적 배너와 바이트 동일. in-turn 헤더(트위스트×그라데이션)는 120ms 틱으로 이미 무한 루프.
- Refactor (repetitive-migration-codemod): src/util/env.ts jeoEnv(suffix, env?) — `(process.env.JEO_X ?? process.env.JEO_X)` 인라인 이중 읽기를 단일 헬퍼로 치환(storage 4, state 7, tools 1, mcp/tools 1, gemini 1, auth 1, catalog 2 + launch 스윕 게이트 2). ?? 시맨틱 보존(빈 문자열은 값). 동시 패스가 편집 중인 핫 파일(slash/autocomplete/markdown-text + launch 잔여 사이트)은 이번 라운드 제외 — 다음 패스에서 마저 이관.
- MRU 기본값(요청 재확인): rememberModelPatch → defaultModel/recentModels 영속 경로 무변경, model-recency·subagent-apply-target 테스트 스위트 내 통과로 검증.
- Tests: env-helper(우선순위/폴백/?? 시맨틱), sweep v2(BSU/ESU 원자성·리와인드 높이·frames*cycles+1 리페인트·일정 케이던스·최종 프레임 패리티).
- Verified: tsc 0; bun test 1096 pass / 0 fail (140 files).
[2026-06-12] Vercel agent-skills ecosystem tolerance (gjc-계승 cycle 1)
- skillDirs: +~/.claude/skills, +~/.gjc/agent/skills, +<cwd>/.claude/skills (Vercel `npx skills add` agent-targeted installs + gjc skill tree); $HOME now wins over Bun's cached os.homedir() for all home-rooted dirs.
- loadSkills: SYMLINKED skill dirs/files followed via fs.stat (Dirent.isDirectory() is false for links — every `npx skills add` linked skill was silently dropped); broken symlinks skipped.
- parseSkillMarkdown: opt-in preferMetaName — frontmatter `name:` is the authoritative identity for EXTERNAL skills (agent-skills standard); sanitized to a bare token, bundled skills keep constructor names. byName keying + /skill command + alias inference follow the resolved name.
- Verified: typecheck 0; skills suites 39 pass / 0 fail (+5 new in test/skills-vercel-compat.test.ts); live `jeo skills` now lists the full ~/.agents/skills catalog (300+ skills incl. ooo/autopilot/survey/spec-stack) + gjc spec-stack, prompt injection stays capped by MAX_SKILLS_PROMPT_LINES.
[2026-06-11] Pass — startup mouse-tracking hygiene ("실행 시 마우스 클릭된 상태" 수정)
- 원인: jeo 자신은 xterm 마우스 트래킹을 켜지 않지만, 이전 프로그램 크래시/스테일 tmux 페인이 ?9/?1000/?1002/?1003(+?1005/?1006/?1015/?1016 인코딩)을 켜둔 채 남기면 첫 프롬프트부터 클릭·모션이 이스케이프 시퀀스로 stdin에 보고됨 — "시작부터 마우스가 눌린 상태"로 보이는 `[<0;…M` 가비지의 정체.
- 수정: terminal.ts resetMouseTracking() — 8개 모드 전부 reset(l) 폼으로 방어적 해제(클린 터미널에선 no-op, enable(h) 폼 절대 미포함을 테스트로 고정); launch 인터랙티브 시작 시 어떤 렌더보다 먼저 1회 방출(TTY 전용). 기존 가드와 합산: queuePromptInputChunk의 ESC 청크 거부 + 프롬프트 전 tty 드레인이 누출 경로를 이미 차단.
- Verified: tsc 0; bun test 1102 pass / 0 fail (141 files).
[2026-06-12] Pass — 실동작 검증 + 라이브 소스 직링크 배포 결함 수정 ("반영 안 됨"/"입력 막힘"의 진짜 원인)
- 원인 규명: ~/.bun/bin/jeo(및 ~/.local/bin/jeo)가 워킹트리 src/cli.ts에 직링크 — 병행 에이전트가 launch.ts/slash.ts를 편집 중인 순간 실행하면 미완성 상태가 그대로 실행됨. "수정이 반영 안 된다"(편집 전 스냅샷 아님, 편집 중 상태)와 "입력 텍스트가 막혔다"(입력 게이팅 코드의 중간 편집 상태)가 모두 이 배포 결함의 증상.
- 수정: 그린 커밋(5faaf3e)에서 `bun build --compile` → dist/jeo 바이너리 생성, ~/.bun/bin/jeo·~/.local/bin/jeo·jeo 심링크를 바이너리로 교체. 이후 워킹트리 상태와 무관하게 안정 스냅샷 실행; 새 패스 반영은 `bun run build` 1회.
- 실동작 검증(tmux PTY): ① 입력 에코 — 한글("입력 테스트 typing-ok") 즉시 표시, ② /help 커맨드 턴 후 타이핑 정상 표시, ③ script PTY 녹화 첫 400바이트가 정확히 마우스 리셋 시퀀스(?9l ?1000l ?1002l ?1003l ?1005l ?1006l ?1015l ?1016l 각 1회, 어떤 렌더보다 선행) — "실행 시 클릭된 상태" 수정의 실기기 증거.
[2026-06-12] Pass — "repeated 'write' 3× 중단" 근본 해결: 교정 바운스 + 미드턴 컨텍스트 가드
- 현장 증상: 65스텝 / 입력 2,005k 토큰 후 "Stopped: repeated the same 'write' call 3×". 근본 원인 2개: ① 미드턴 컴팩션 부재 — maybeCompact는 턴 경계에서만 돌아 한 턴 안에서 히스토리가 무한 성장(스텝당 ~31k 입력), 비대해진 컨텍스트가 모델을 반복 루프로 퇴화시키고 비용을 복리화; ② 반복 가드가 교정 기회 없이 즉사 — 성공한 write 직후 모델에게 "이미 됐으니 done을 보내라"고 말해주는 장치가 전무.
- 수정 ①(engine): repeatCount==2에서 중복 호출을 실행 없이 스킵하고 교정 메시지 주입("동일 호출은 재실행되지 않았다 — 완료면 done, 아니면 다른 행동"); 교정을 뚫고 3회째 반복할 때만 종료(메시지도 "even after an explicit correction"으로 정직하게). 변이 도구는 어떤 경로로도 재실행되지 않음(테스트로 고정).
- 수정 ②(engine+compaction): trimToolResultsInPlace — 추정 토큰이 maxHistoryTokens(기본 80k, 옵션) 초과 시 가장 오래된 Tool-result 본문을 결정적으로 스텁화(최근 8개 보존, system/실프롬프트/assistant 불가침, 메시지는 교체-비변이로 identity 토큰캐시 정합). 매 스텝 시작에 가드 + onNotice 1줄.
- Tests: repeat-bounce-and-trim(교정 후 done 성공 / 교정 관통 시 정지 / write 단일 실행 보장 / trim 예산·최근보존·불가침·no-op), engine.test 반복 계약 갱신.
- Verified: tsc 0; bun test 1105 pass / 0 fail.
[2026-06-12] gjc-계승 마라톤 cycles 2-4 (합의 문서: plan/gjc-inheritance.md)
- Evidence: gjc v0.4.3 소스 심층분석 2건(architect 서브에이전트 — hashline/도구프로토콜/task경계 + 검증가드/메모리/확장성) + pi-mono/hermes/nullclaw/zeroclaw 철학 리서치. critic 합의 라운드 1(ITERATE→반영): 순서 B3→[B7+B3.5]→B4→B2→B6 확정, B2 dual-protocol 리스크 명세, B4 escape hatch 명세.
- cycle 2 (B3): WORKING_DISCIPLINE(gjc completion-contract+tool-priority, ~180tok) — engine executorSystemPrompt + launch 인터랙티브 프롬프트 + subagent 템플릿 3곳 배선.
- cycle 3 (B7+B3.5): file-freshness guard — read가 mtime/size 스냅샷 기록(LRU 64), edit/write가 외부 변경 감지 시 1회 거부+현행 내용 재제시(recovery)+스냅샷 갱신으로 즉시 재시도 허용; 자기 변경은 비-stale. SEARCH mismatch 에러에 앵커 주변 현행 발췌 동봉. test/edit-freshness.test.ts 5종.
- cycle 4 (B4): done-verification guard — write/edit 성공 턴에서 검증 신호 없이 done → 1회 푸시백, 2번째 done 통과(escape hatch). test/done-verification-guard.test.ts 4종.
- Verified: typecheck 0; full bun test 1128 pass / 0 fail.
[2026-06-12] gjc-계승 마라톤 cycle 5 (B2 hashline-lite)
- read 출력 전 라인에 2자 콘텐츠 앵커 부여: `42ab|text` (lineAnchor: trailing ws/CR 무시, Bun.hash 36^2 버킷 — 내용만으로 해시되어 형제 편집으로 라인이 밀려도 유효).
- edit ≔ 경로 앵커 검증: `≔12ab..15cd`/`≔7xy+` — 모델이 인용한 앵커가 현행 라인 해시와 불일치하면 거부 + 현행 발췌 재제시(silent corruption 차단). 앵커 없는 ≔A..B는 기존 그대로(back-compat).
- dual-protocol 픽스업(critic 합의): SEARCH/REPLACE 블록에 read 앵커 prefix가 통째로 복사된 경우(전 라인 prefix 패턴) 스트립 후 매칭 — 약한 모델의 copy-paste 함정 무해화.
- 프로토콜 문서 갱신(engine TOOL_PROTOCOL + launch TOOL_DESCRIPTIONS), 포맷 고정 테스트 7곳 anchor-agnostic 전환, test/hashline-lite.test.ts 6종 신규.
- Verified: typecheck 0; full bun test 1134 pass / 0 fail.
[2026-06-12] gjc-계승 마라톤 cycles 6-7
- cycle 6 (B6, hermes 경험→증류): src/agent/memory.ts 신규 — 세션 종료 시(턴≥1) 1콜 증류로 .jeo/memory/MEMORY.md에 누적 학습(repo facts/동작 명령/gotchas/유저 선호, 기존 문서와 merge, 6K자 캡, atomic write, 20s 타임아웃, 실패 무해). 다음 세션 시스템 프롬프트에 <project_memory> 블록 3K자 캡 주입. JEO_NO_MEMORY=1 비활성. launch 종료 경로+프롬프트 조립 배선. test/memory.test.ts 3종.
- cycle 7 (B8, gjc CompactionDetails 계승): extractTouchedFiles — 요약될 메시지에서 write/edit tool-call의 filePath를 기계 추출(중복 제거, 20개 캡)해 컴팩션 요약 프롬프트에 "PRESERVE verbatim"으로 핀 — 요약 후에도 파일 맥락 유지. test/compaction-touched-files.test.ts 2종.
- Verified: typecheck 0; full bun test 1139 pass / 0 fail. 잔여 백로그: B5(task-spawn bashPrefixes), B9(fan-out 정당화), B10(spill 설정화) 후 critic 합의 라운드 2.
[2026-06-12] gjc-계승 마라톤 cycle 8 (B9+B10) — 라운드 1 백로그 소진
- B9 spawn-gate lite: task fan-out >4는 런칭 전 거부, "justification"(≥20자) 동봉 시 허용 — gjc spawn-gate의 정당화 강제를 프로토콜 레벨로 경량 계승(기존 silent capping 제거).
- B10: JEO_TOOL_OUTPUT_MAX 환경변수로 모델 가시 출력 캡 조정(500..200K 클램프, 기본 4000); TOOL_SPILL_THRESHOLD가 캡을 추적해 잘린 내용은 항상 artifact 복구 가능.
- Verified: typecheck 0; full bun test 1141 pass / 0 fail. test/spawn-gate-lite.test.ts 2종 신규.
- 라운드 1 결산: 8사이클 / 신규 테스트 25종 / 계승 완료 = 생태계 관용·completion-contract·freshness+복구·done 가드·hashline-lite·경험 증류·컴팩션 파일보존·spawn-gate·출력 설정화. B5(task-spawn bashPrefixes)는 critic 판정대로 라운드 2 재설계 대상. 라운드 2 후보: 도구 동시성 확대, 컴팩션 핸드오프, _i intent, LSP-lite.
[2026-06-12] Pass — `jeo --tmux` 동작 복구 (compiled-binary 가상 argv 버그)
- 증상: `jeo --tmux` → "Starting new tmux session: …" 직후 "can't find session" — 세션이 생성 즉시 사망.
- 근본 원인: 컴파일 standalone 바이너리에서 process.argv[1]은 디스크에 존재하지 않는 Bun 가상 경로(/$bunfs/root/cli) — 기존 코드가 이를 inner 명령 엔트리포인트로 그대로 사용해 `tmux new-session`의 명령이 spawn 즉시 실패, 세션이 죽고 후속 attach가 "can't find session"으로 표면화. (소스 실행 bun src/cli.ts 경로에서는 재현 안 됨 — dist/jeo 전환 후 노출된 버그.)
- 수정: tmuxLaunchCommand(argv1, execPath, cwd) 순수 헬퍼 — /$bunfs(또는 Windows B:\~BUN) 가상 경로/빈 argv1이면 바이너리 자신(execPath)을 실행, .ts/.js/.mjs는 런타임 경유, 그 외 shim/바이너리는 직접 실행.
- 실동작 검증(plain PTY, TMUX unset): jeo --tmux → 세션 jeo-projects-* 생성·생존, 페인에 입력박스 렌더, `mouse on`+@jeo-profile/@jeo-project 프로파일 적용 확인. dist/jeo 재빌드로 즉시 반영.
- Tests: tmuxLaunchCommand 3형태(가상/소스/shim) 계약 고정.
- Verified: tsc 0; bun test 1150 pass / 0 fail.
[2026-06-12] gjc-계승 마라톤 라운드 2 (cycles 9-12) — critic 합의 배치 소진
- cycle 9 (hashline 3-way 재매핑): editTool ≔ 경로에서 anchor mismatch 시 ±64 윈도 내 동일 content-anchor 라인 탐색 → 유일매치면 재매핑(범위는 양끝 동일 delta 강제, 충돌/미발견은 기존 거부+현행 재제시 fallback). **추가 발굴/근본수정**: 전수치 anchor(`68` 등, 약 7.7%)가 `≔1`+`68`→`≔168`로 병합되어 검증 자체를 무력화하던 사일런트 손상 버그 — lineAnchor를 letter-leading(a-z 선두 + 0-9a-z) 인코딩으로 변경(26×36=936 공간). test/hashline-remap.test.ts 7종.
- cycle 10 (B5 재설계): SubagentRole.bashAllowedPrefixes — 설정된 mutating 역할은 subagentToolset()이 bash를 prefix-검사 래퍼로 교체. bashCommandAllowed()는 `; && || |` 세그먼트 분해 후 각 세그먼트 검사(env-assignment/sudo 스트립, `$(`·백틱 거부). 레지스트리 정의=런타임 제약(config 불필요), 번들 역할 동작 불변(executor=full bash, RO=bash 제거). test/subagent-bash-allowlist.test.ts 5종.
- cycle 11 (컴팩션 핸드오프): extractTouchedFiles가 `Tool [bash] result` 보수 패턴(created/wrote/written to/deleted/removed + path-shape 필터)도 수집 + 요약 메시지 선두 "Files touched:" 헤더 강제 + CompactionResult.touchedFiles 표면화 + launch.ts 자동/수동 양 경로에서 요약 실패 placeholder에 파일목록 보존·콘솔 표기. test +2종.
- cycle 12 (쓰기 병렬화): 엔진 배치 그룹핑 read/write/exclusive 3종 재설계 — 서로 다른 파일 write/edit 병렬, 같은 파일/경로미상은 순차 경계, bash는 항상 exclusive. read·write 그룹 분리로 read↔write 레이스 원천 차단. test/write-parallel.test.ts 4종(동시성 실측).
- Verified: typecheck 0; full bun test 1161 pass / 0 fail. 라운드 2 결산: 4사이클(+1 버그픽스), 신규 테스트 18종. 라운드 3 후보: 잔여 hashline 강건성, post-edit bash tsc 훅(LSP-lite 대안), _i intent 외부 텔레메트리, cross-file read+write 혼합 동시성.
[2026-06-12] gjc-계승 마라톤 라운드 3 (cycle 13) — critic ITERATE→OKAY 합의
- cycle 13 (post-turn 훅 진단 피드백): 핵심 발견 — jeo는 이미 config-driven hooks(pre-tool/post-turn/post-implementation, match.tool)를 갖췄으나 runPostTurnHooks가 void라 post-edit `tsc`/lint 훅의 실제 진단이 모델에 도달 못 하고 advisory notice만 발생. 수정: runPostTurnHooks가 exit≠0 훅의 출력을 PostTurnHookDiag[]로 반환 → engine processAndPushResults가 해당 도구 결과 블록에 `[post-turn hook "<run>" — exit N]:\n<output>` 첨부(모델 가시, 도구 ok/fail 불변). gjc post-edit 진단 가치를 기존 확장점으로 실현(신규 의존성/코어변경 0, pi-mono 준수).
- critic 5개 명확화 반영: exit≠0에만 표면화(A1), 배치 내 동일출력 엔진측 dedup+교차참조(A2), timeout/abort는 부분출력 없음(A3), JEO_TOOL_OUTPUT_MAX 독립 truncate(A4), match.tool 정확일치(A5). 가드: 훅 실패가 도구 성공/실패·step-budget에 영향 없음.
- 검증: typecheck 0; full bun test 1169 pass / 0 fail (test/post-turn-feedback.test.ts 6종 — 비정상종료 진단반환/정상종료 무진단/타임아웃 무부분출력/match.tool 게이팅/엔진 첨부+ok유지/배치 dedup). docs/improvements.md 라운드 2-3 항목 추가, plan/gjc-inheritance.md 라운드 3 렛저.
- 누적: 13사이클(라운드 1~3). 라운드 4 후보(미합의): match.tool `|`-다중매칭, post-turn surfaceOnSuccess 옵트인, 출력 minimizer 훅화.
[2026-06-12] Pass — 텍스트 입력 딜레이 근본 해결 (키스트로크당 execSync 프로세스 스폰 제거)
- 근본 원인(실측): previewLines/statusBarLine이 매 키 입력마다 실행되며 resolveTheme(process.env)를 최대 3회 호출 — 테마 미설정(기본) 상태에서는 호출마다 ① ~/.jeo/config.json 동기 existsSync+readFileSync+JSON.parse, ② macOS detectAppearance의 execSync("defaults read -g AppleInterfaceStyle") 프로세스 스폰(실측 11.8ms/회) → 키당 ~35ms+α. 한글 조합 입력은 글자당 다중 키이벤트라 체감 지연이 더 큼.
- 수정 3중: ① color.ts detectAppearance 프로세스-수명 메모(COLORFGBG 키드 — 팔레트 광고 터미널은 env별 존중; resetAppearanceCache 테스트 훅), ② themes.ts getExplicitThemeFromConfig 메모(env 테마가 항상 우선이라 /theme 즉시성 유지; resetThemeConfigCache 훅), ③ launch.ts 키-핫 콜사이트 호이스팅 — uiTheme/uiAccent/uiAccentShadow 1회 해석, /theme 변경 시 refreshUiTheme()로만 갱신(상태바·입력박스 즉시 반영으로 UX도 개선).
- Tests: 워밍 50회 < 콜드 1회(스폰 1회 보장), resolveTheme 300회(≈100키×3) < 30ms(구버전 1키 예산 미만), COLORFGBG dark/light 판정 유지.
- Verified: tsc 0; bun test 1163 pass / 0 fail.
[2026-06-12] gjc-계승 마라톤 라운드 4 (cycle 14) — architect 발굴 4픽스 + 다중매칭
- architect 리뷰(agent://5-Round4Discovery, WATCH): 에이전트 코어 심각도 평가 → Med 4건 채택.
- F1 훅↔done guard 결합: 이전 스텝 bash 검증이 있어도 최신 편집의 red tsc 훅이면 done 푸시백(훅 명명, escape hatch 유지). runPostTurnHooks {diags, ran} 반환, pendingHookFailure 추적(red=설정, clean 완주=해제).
- F2 원자적 workflow state: writeWorkflowState temp+rename — torn write가 fail-closed mutation guard를 영구 wedge하던 zeroclaw 위반 해소.
- F3 쓰기병렬화 dedup 키 정규화: raw 문자열 → path.resolve+lowercase. `./x.ts` vs `x.ts`/대소문자 변형의 사일런트 lost-update(macOS 실워크스테이션) 차단.
- F4 메모리 주입 방어: MEMORY.md 주입 시 </project_memory> 태그 중화 + DATA-not-instructions 프레이밍(지속형 prompt-injection 벡터 차단).
- 14a match.tool `|`-다중매칭(hookMatchesTool) — 라운드3 critic 명시 승인 follow-up.
- Verified: typecheck 0; full bun test 1177 pass / 0 fail (+8종: post-turn-feedback F1 2종+반환형 갱신, write-parallel 별칭/대소문자 2종, memory 펜스 1종, state-command 원자성 1종). 렛저: plan 라운드4, improvements.md.
- 커밋: cycle 13은 67218e6로 푸시 완료. cycle 14는 본 패스에서 커밋·푸시.
[2026-06-12] Pass — 모델 선택 3단계 완성: role 타깃 + Reasoning(thinking) 메뉴 (gjc 실관찰 이식)
- gjc /model 실행·관찰(tmux): 모델 리스트(역할 태그 + "(xhigh)/(inherit)" 표기) → "Action for <model>"(DEFAULT/EXECUTOR/ARCHITECT/PLANNER/CRITIC) → "Reasoning for <Role>"(off~xhigh, 토큰 설명) 3단계 확인.
- jeo 이식: /provider 인터랙티브 픽 → 기존 apply-target(역할) 피커 다음에 Reasoning 피커 추가 — minimal/low/medium/high/xhigh(+역할 대상엔 "inherit — follow default"), 현재 레벨 hint 마킹, 라벨에 thinkingMaxTokens 기반 ~Nk tokens 설명. ESC=현행 유지.
- 영속: default 대상 → sessionThinking 즉시 반영 + config.thinkingLevel 저장; 역할 대상 → subagents[role].thinking(스키마/Config 타입 확장; inherit 선택 시 override 제거). task-tool이 스폰 시 resolveSubagentThinking ?? thinkingLevel → thinkingMaxTokens로 maxTokens 적용 — 역할별 추론 예산이 실제 서브에이전트 실행에 반영.
- applyTargetChoices 힌트가 gjc식 "(level)/(inherit)" 표기 — 피커가 역할별 모델+추론 현황판 역할.
- 병행 패스의 provider Round-5(usage 단일 보고, 빈 응답 원인 표면화)와 합산 그린.
- Tests: resolveSubagentThinking/withSubagentSetting(override·inherit·clear), applyTargetChoices thinking 태그, 기존 힌트 테스트 신계약 갱신.
- Verified: tsc 0; bun test 1181 pass / 0 fail.
[2026-06-12] gjc-계승 마라톤 라운드 5 (cycle 15) — 프로바이더/스트리밍 레이어 (architect 발굴)
- 축 전환: 에이전트 코어(라운드 1~4) → 프로바이더 경계. architect 리뷰(agent ref 6-Round5Providers, WATCH) HIGH 2건+MED 1건 채택.
- #1 빈완료 균일 계약(HIGH): anthropic/openai/codex/ollama가 200-with-no-text를 ""로 반환 → 엔진 bounce가 자기종료 불가, reasoning+작은 maxTokens 조합이 과금 스텝버짓 전소. 4개 어댑터 모두 gemini 계약 미러링: no-text 시 stop_reason/finish_reason/incomplete_details/done_reason 동봉 throw + "raise maxTokens" 힌트.
- #2 디스커버리 데드라인(HIGH): Cloud Code Assist 프로젝트 디스커버리 fetch 3개에 boundedSignal(턴 abort ∧ 30s/요청) — 첫 OAuth 턴이 stalled TCP로 영구 행하던 벡터 봉합. antigravity/gemini 호출자 options.signal 스레딩.
- #3 anthropic usage report-once(MED): message_start 2회보고 → input 2배 과대계상 제거(start=캐시만, delta=1회 보고). 옛 계약 고정 테스트 1건 갱신.
- F6(라운드4 보류): 스텝 성패를 non-trivial 콜로 판정 — read(ok)+edit(fail) 반복의 MAX_FAILURES 우회 봉합. engine-multitool 테스트 2종.
- Verified: typecheck 0; full bun test 1190 pass / 0 fail (+13: provider-empty-completion 9종 신규 등). plan 라운드5 렛저, improvements.md 갱신.
[2026-06-12] gjc-계승 마라톤 라운드 6 (cycle 16) — 컨텍스트 오버플로 반응적 복구
- #4(라운드5 보류분): isContextOverflowError(context_length/prompt too long/413 등) 신설 + friendlyProviderError에 overflow→"/compact" 안내·404→model-not-found "/model" 안내 케이스 추가 — 불투명한 raw 400/404 본문 수집 종료.
- 엔진 반응적 복구: provider가 overflow를 보고하면(로컬 추정이 빗나간 케이스 — 이미지/토크나이저 편차) trimToolResultsInPlace(예산 절반, keepRecent 2)로 1회 트림 후 같은 스텝 free retry. 2번째 overflow 또는 트림 불가 시 friendly 에러로 턴 종료(one-shot 플래그).
- Verified: typecheck 0; full bun test 1194 pass / 0 fail (+4종 provider-error-taxonomy: 감지기 패턴/taxonomy 문구/트림+재시도 성공/2차 overflow 표면화).
[2026-06-12] Pass — /subagent 슬래시 명령 + 구성 패널 thinking 표기
- /subagent(및 /subagents) 별칭 추가 → /agents 핸들러 전체 재사용(레지스터 별칭 패턴, /login→/provider login과 동일); 슬래시 팔레트에 "/subagent [role]" 등재(Subagents 그룹).
- formatAgentsPanel/formatAgentDetail에 thinking 컬럼: 역할별 명시 레벨 또는 (inherit) 표기, 디테일 블록은 "inherit (follows the default thinking level)" 설명 — /agents·/subagent가 모델·thinking·스텝·read-only까지 현 구성 현황판 완성.
- 실동작 검증(tmux, 샌드박스 config): /subagent → "executor — claude-opus-4-5 (xhigh) ≤20 steps / planner (inherit) …" 패널 실표시.
- Verified: tsc 0; bun test 1198 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] gjc-계승 마라톤 라운드 7 (cycle 17) — 워크플로 렛저 무결성 (architect 발굴 HIGH 2건)
- team stale-state 리셋(HIGH): plan A 완료 후의 team-state(pending=[])가 plan B에 재사용되어 plan B가 no-op으로 "All tasks executed successfully" 가짜 성공을 기록하던 구멍 — plan_path/slug 불일치 시 새 플랜 태스크로 재초기화. 같은 플랜 재개(resume)는 불변. 완료 시 active=false 플립(죽은 필드 정리).
- ultragoal 검증 연극 제거(HIGH): 기준마다 동일 글로벌 bun test(run/cli 텍스트 포함 시 무조건 green인 --help)를 돌려 조작된 기준별 ✅/❌ 매트릭스를 렛저에 기록하던 구조 — suite 1회 실행, 기준은 ⚠️ UNVERIFIED 정직 기록, status SUITE_GREEN/FAILED + suite_green 필드, 리포트 temp+rename 원자 쓰기. "검증이 실패할 수 없는 검증"은 검증이 아니다.
- openai stream_options 호환(라운드5 #5): OpenAI-호환 로컬 백엔드가 옵션 필드에 400 시 1회 스트립 재시도 — 무관한 400은 블라인드 재시도 없음.
- Verified: typecheck 0; full bun test 1202 pass / 0 fail (+8: workflow-integrity 4종 — 새플랜 재시작/동일플랜 재개/정직 검증/red suite, provider-empty-completion +2, 기타). plan 라운드7 렛저, improvements.md 갱신.
[2026-06-12] Pass — 팝업 Tab 자동완성 + /btw 스트리밍 개선
- Tab 자동완성: 입력창에서 `/`·`$` 팝업 리스트가 떠 있을 때 Tab이 하이라이트 행(화살표 선택) 또는 최상위 매치(프리픽스 우선)로 라인을 완성 — `/mod`+Tab→`/model `, `$sp`+Tab→`$spec-kit `. 후행 공백이 팝업을 닫아 바로 인자 입력. tabCompleteSelection() 순수 헬퍼(slash.ts) + 키프레스 Tab 분기(readline 자체 컴플리터가 먼저 돌아도 rl.line 덮어쓰기로 팝업 선택이 최종 상태; 후보 덤프는 게이트로 차단).
- /btw(직전 패스분): 응답을 토큰 스트리밍으로 즉시 표출(무피드백 10-30초 침묵 = "고장" 체감 해소) + 사이드 질문은 reasoning LOW 캡(xhigh 세션에서 느린 모델이 첫 바이트 전에 거대 thinking 예산을 태우던 문제). haiku/antigravity 실호출 양쪽 `btw> OK` 검증.
- 실동작 검증(tmux): `/mod`+Tab→`/model`, `/ag`+↓+Tab→`/agents` 입력창 실완성 캡처.
- Tests: tabCompleteSelection 계약(하이라이트 우선/최상위 폴백/범위외/빈/$ 동작).
- Verified: tsc 0; bun test 1206 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] gjc-계승 마라톤 라운드 8 (cycle 18) — 오케스트레이션 MED 3건 소진
- (A) 부모측 변이 감사: task-tool·team이 서브에이전트의 성공한 write/edit/bash를 onToolResult에서 실측 카운트 — mutating 역할이 0건 변이로 "완료"하면 "[parent audit] … treat Changed Files claims as UNVERIFIED" 주석(task-tool)/경고(team). 보고서의 Summary:/Changed Files: 마커는 형식 증명일 뿐 작업 증명이 아니라는 원칙의 기계화.
- (B) 크로스 프로세스 런 락: state.ts acquireWorkflowRunLock — O_EXCL 락파일(pid 포함), 死pid stale 락은 1회 인수, 생존 보유자는 actionable 에러로 거부. runTeamEngine 본문 try/finally 래핑 — 동시 jeo team 2개가 pending[0]을 각자 pop해 이중 실행/완료 소실하던 레이스 차단.
- (C) 실패 태스크 마커: 실패 시 current_phase=failed + failed_task 영속(이전엔 성공만 상태를 남김). 다음 런이 "partial edits may be on disk" 경고 후 마커 해제·재개. WorkflowState.failed_task 신설.
- Verified: typecheck 0; full bun test 1206 pass / 0 fail (+3종 workflow-integrity: 락 수명주기, 실패마커+재개경고, parent audit — 실스트림에서 경고 발화 확인). 라운드 4~7 architect 백로그 전부 소진.
[2026-06-12] gjc-계승 마라톤 라운드 9 (cycle 19) — 자기수정 루프 라이브 e2e 실증
- 시나리오: /tmp 샌드박스 프로젝트 + OAuth 사본 config(JEO_CONFIG_DIR, 검증 직후 폐기) + 결정적 post-turn 훅(.jeo/hooks.json, match.tool "edit|write": src/app.ts에 VERSION export 없으면 LINT-E001 + exit 1). 프롬프트는 "훅이 문제를 보고하면 보고된 것만 고쳐라"만 지시 — 훅의 검사 내용은 모델에 비공개.
- 실행(dist/jeo, antigravity/gemini-3.5-flash-low, --no-tui --no-session -p): step2 편집 → 훅 RED(LINT-E001 advisory 라이브 발화) → step3 재읽기 → step4 진단대로 `export const VERSION = "1.0.0"` 정확 추가 → 훅 GREEN → step6 bash 검증 → done 무푸시백. 동적 스텝버짓 novelty 연장도 라이브 발화("progress detected … extended to 15 (extension 1/2)").
- 결과 파일 검증: greet()='hello, jeo' + VERSION export 둘 다 정확. cycle 13(훅 진단→모델 피드백)·14a(다중매칭)·14 F1(pendingHookFailure 해제)·라운드1(동적 버짓)이 실모델 합주로 증명됨.
- 비고: 소스 트리(src/tui/app.ts)는 동시 세션 편집 중이라 빌드 불가 — dist/jeo 바이너리로 우회 실행(이 우회 자체가 standalone 배포 경로 검증이기도 함).
[2026-06-12] Pass — "이전 작업 우선 이어가기" 버그 수정 (new-input-first 계약)
- 근본 원인: promptInput()이 pendingStdinLines에 남은 옛 라인(턴 중 타이핑, Enter 포함 버퍼 노이즈)을 다음 프롬프트로 **무표시 자동 실행** — 새 입력을 넣어도 큐의 이전 작업이 먼저 실행되는 체감("잔여가 남아 이전 작업을 우선 이어감").
- 수정: restoreQueuedLinesToPrefill() — TTY에서는 큐 라인을 자동 실행하지 않고 입력창 prefill로 복원(보이고 편집 가능; Enter=실행, Esc=폐기) + "(restored N queued input line(s)…)" 안내. 파이프/비TTY는 레거시 순차 자동 서빙 유지(launch-repl-eof 계약 보존). 메인 루프 톱에서 매 프롬프트마다 큐를 비워 서브 프롬프트 오염도 차단.
- 동시 패스 테마 확장(aurora/synthwave/sakura + accentShadow 색조 + diff 팔레트)의 미정렬 테스트 2건 정합.
- Tests: new-input-first 3케이스(전량 드레인·prefill 결합 순서·공백 제거·no-op·chunk→line→prefill 엔드투엔드).
- Verified: tsc 0; bun test 1213 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] Pass — jeoEnv 이관 마감 + README 최신화 (이어서 진행)
- jeoEnv 마이그레이션 완료: 보류했던 핫사이트 잔여분 이관 — launch.ts 3곳(TMUX_LAUNCHED ×2, NO_SLASH_PREVIEW) + app.ts TUI_ALT_SCREEN(레거시 단일 JEO_ 읽기였던 곳도 JEO_ 우선 폴백 획득). 인라인 (JEO_X ?? JEO_X) 패턴 소스 전체 0건.
- README ×4: 최근 UX 배치 문서화 — 3단계 모델 픽(역할+Reasoning, inherit), /subagent 구성판, Tab 팝업 자동완성, /btw 스트리밍, new-input-first 입력 계약, aurora/synthwave/sakura 테마.
- Verified: tsc 0; bun test 1218 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] gjc-계승 마라톤 라운드 10 (cycle 20) — 플래닝 프런트엔드 무결성 (architect BLOCK→fixed)
- ralplan write-time 패리티(HIGH): isValidPlan에 role 검증 추가(getSubagentRole) — role "developer" 같은 흔한 LLM 편차가 write-time을 통과해 team에서야 abort하던 늦은 실패 제거. 전 패스 무효 시 파일은 검토용으로 저장하되 current_phase=complete 마킹 거부 + ok:false(기존엔 WARNING만 내고 complete).
- stale 인터뷰 차단(HIGH): freezeSeed가 active=false 플립 + 완료 상태에서 다른 새 아이디어 인자가 오면 clear 후 신규 인터뷰 — `jeo deep-interview "idea B"`가 idea A를 조용히 재사용해 체인 전체가 옛 아이디어를 실행하던 사일런트 요구사항 소실 봉합.
- approve 게이트화(MED): 승인 전 PlanSchema+role 검증(team과 동일 계약) — 고무도장 제거, 불합격 평면은 실행 전 거부.
- ralplan "consensus" 정직 리라벨(HIGH의 interim): 단일모델 3패스 프롬프트임을 로그에 명시(critic 평결 게이트 없음 — 라운드7 ultragoal과 동형). 전면 수정(실 서브에이전트+평결 게이트)은 라운드 11 후보.
- LOW: PlanSchema가 depends_on/after/needs 등 dependency-형 키 거부(직렬 실행기가 존중 못 하는 순서 제약의 환상 차단), spawn-gate 동시성 명확화 주석.
- Verified: typecheck 0; full bun test 1222 pass / 0 fail (+5종: ralplan refuse-complete/정상 완료, approve 거부, 인터뷰 신규 아이디어 재시작, deps 키 거부).
[2026-06-12] gjc-계승 마라톤 라운드 11 (cycle 21) — ralplan 합의의 실체화
- runConsensusCriticGate 신설: read-only critic 서브에이전트가 repo 접근(read/search/find/ls) 하에 후보 플랜을 시드+실저장소 대조 검토. 평결 계약 강제([OKAY]/[ITERATE]/[REJECT] + Justification, validateSubagentDoneReason) — 미충족은 unverified로 fail-closed. [ITERATE]는 1회 수정 라운드(critic 근거를 드래프터에 피드백) 후 재심.
- 평결 영속: ralplan state에 consensus/consensus_detail 기록. [OKAY]만 current_phase=complete. 비승인 시 플랜은 검토용 저장 + 근거 표면화 + ok:false.
- approve 강화: 영속 평결 consensus==="okay" 요구 — 평결 없는/실패한 플랜은 승인 불가(구 상태는 ralplan 재실행으로 치유). 라운드 10에서 "정직 리라벨"로 보류했던 #1(합의 연극)의 전면 수정 — 차단 가능한 게이트가 진짜가 됨.
- Verified: typecheck 0; full bun test 1224 pass / 0 fail (신규/갱신 4종: OKAY 완료+평결 영속, REJECT 차단+근거 영속, approve 평결 거부, approve fixture 갱신). 체인 전 게이트 실체화 완료: write-time → 합의 → 승인 → 실행 → 검증.
[2026-06-12] gjc-계승 마라톤 라운드 12 (cycle 22) — 시드 라운드트립 (마지막 MED 소진)
- src/agent/seed.ts 신설: deep-interview의 작성기(yamlList)와 ultragoal의 파서(parseSeedAcceptanceCriteria)가 한 모듈·한 인코딩(JSON encode↔decode) 공유. 구 인라인 파서는 replace(/"/g,'')로 모든 큰따옴표를 strip — `Display "Done" message` 류 기준이 검증 렛저에 사일런트 맹글링되던 문제 해소. 레거시 비인용 항목은 외곽 따옴표쌍만 strip하는 관용 폴백 유지.
- freezeSeed 라운드트립 자기검증: 기준이 파서를 정확히 통과하지 못하면 freeze 거부 — 미래 작성기/파서 드리프트가 렛저를 조용히 오염시키는 대신 시끄럽게 실패.
- Verified: typecheck 0; full bun test 1228 pass / 0 fail (+4종 seed-roundtrip: 따옴표/콜론/백슬래시/유니코드 왕복, 레거시 관용, 섹션 경계, e2e 리포트 비맹글링).
- 결산: architect 발굴 MED 이상 백로그 전부 소진. 잔여 LOW/WATCH 2건뿐.
[2026-06-12] gjc-계승 마라톤 라운드 13 (cycle 23) — 강화된 체인 라이브 풀 e2e
- 전 체인 실모델 관통: deep-interview --auto → ralplan → approve → team(×2) → ultragoal. 신선 빌드 dist/jeo(R10~12 게이트 포함), /tmp 샌드박스, OAuth config 사본 즉시 폐기. 과제: slugify 모듈+테스트 그린필드.
- 라이브 발화 확인된 게이트: ① 실 모호성 스코어링 60%→10%(인터뷰가 유니코드/대시 엣지케이스를 스스로 질문) ② critic 서브에이전트 [OKAY] + "repo is nearly empty…" 실저장소 열람 근거(R11 합의 실체화) ③ approve 3중 게이트(스키마+role+평결) ④ team 1차 스텝버짓 소진 → fail-closed 정지+failed_task 영속(R8) → 2차 "[WARN] partial edits" 재개 경고 후 4/4 완료 ⑤ ultragoal SUITE_GREEN + 6기준 ⚠️ UNVERIFIED 정직 기록(R7) + 따옴표 기준 비맹글링(R12).
- 산출물 실검증: slugify(NFD 정규화·하이픈 붕괴·트림·빈문자) 정확 구현, bun test 6 pass / 0 fail 직접 확인.
- 특기: team 1차 실패는 결함이 아니라 계약 동작 — 작업은 됐지만 done 계약 미신고면 성공을 주장하지 않는다(completion-contract의 라이브 증명). 재개 경로가 그 실패를 정확히 흡수.
- 마라톤 양 축 완결: 메커니즘 구축(R1~12, 1228 tests) + 라이브 실증(R9 코어 루프, R13 풀 체인).
[2026-06-12] gjc-계승 마라톤 라운드 14 (cycle 24) — 잔여 LOW/WATCH 소진 + 라이브 실검증
- #7 슬로우드립 스트림 데드라인: StreamIdleOptions.deadlineAt(전체 wall-clock)을 per-chunk idle과 합성 — 토큰이 idle 한도 직전마다 똑똑 떨어지는 스트림은 기존엔 무한 실행. JEO_STREAM_MAX_MS 옵트인(기본 OFF — "긴 활성 생성을 죽이지 않는다" 트레이드오프 유지), streamText 스트림 경로 배선, streamMaxMs() env 파서 export.
- LOW #6 기준 품질 플로어: freezeReadiness가 vague-only 기준("It works"/"ok"/"동작한다", <8자)만으로는 freeze 거부 — 보수적(실질 기준 1개면 통과, 비영어 안전).
- Verified(유닛): typecheck 0; full bun test 1232 pass / 0 fail (+4종: 슬로우드립 중단/건강 스트림 무영향/env 파싱/vague 거부).
- Verified(라이브): dist 재빌드 → ① chat(스트리밍)+JEO_STREAM_MAX_MS=50 → "stream exceeded the overall deadline (JEO_STREAM_MAX_MS)" 라이브 발화 ② 기본 OFF chat 정상 "hello" ③ launch -p(비스트리밍 설계) env 무관 정상 = 노브가 정확한 경로에만 적용. config 사본 즉시 폐기.
- 결산: architect/critic 발굴 백로그 전량(HIGH/MED/LOW/WATCH) 소진 — 마라톤 24사이클.
[2026-06-12] 라운드 15 (cycle 25) — 모델 디스커버리 실측 수리 (openai 재확인 + gemini 라이브 리스트)
- 라이브 프로브로 실증한 구조 문제 2건: ① Codex 모델 엔드포인트가 client_version 쿼리를 요구하게 변경(없으면 HTTP 400, 낮은 버전은 빈 배열) — jeo의 openai OAuth 라이브 디스커버리가 통째로 죽어 카탈로그 폴백(2개)만 노출되던 상태 ② gemini /v1beta/models 기본 페이지=50+nextPageToken — 단발 fetch가 2페이지를 묵살(실측 50+token vs 전체 55).
- 수정: CODEX_MODELS_URL에 client_version=2.0.0(실측: 0.46→[], 0.99→gpt-5.4, 1.0/2.0→gpt-5.5 풀셋; 주석에 실측 기록) + codex-auto-review 류 리뷰 전용 모델 제외. gemini URL pageSize=1000 + listProviderModels가 nextPageToken 추적(≤4페이지, 실패 시 부분 리스트 유지). isGeminiChatModel에 lyria(음악)·nano-banana(이미지) 가족 추가(generateContent는 지원하나 채팅 턴 불가). antigravity는 api-key 스왑 면제(리스트 엔드포인트가 OAuth 전용 — 잠재 버그 가드).
- 라이브 재검증: openai 400→ok 3모델(gpt-5.5/5.4/5.4-mini), gemini api_key 21개 클린 채팅 모델(3.5-flash/3.1-pro 등 최신 포함, 누수 0), antigravity 8개 무손상.
- Verified: typecheck 0; full bun test 1236 pass / 0 fail (+4종: client_version URL 계약, pageSize 계약, auto-review 제외, nextPageToken 추적 모킹).
[2026-06-12] Pass — $스킬 호출 카드 ([skill] 블록) 노출
- gjc 패리티: $name//skill 호출 시 작업 시작 전 [skill] 카드 노출 — Skill 이름 / 해석된 SKILL.md 경로(디스커버리 우선, 번들은 "(bundled) src/prompts/skills/<name>/SKILL.md") / 주입되는 Prompt 라인 수(공백 제외). 워크플로우·프롬프트 스킬 양 경로 공통(runSkillInvocation 톱), forge 카드 + 테마 투톤으로 렌더.
- catalog: SkillDoc.sourcePath — 디스커버리 로더(단일 .md / 디렉터리 SKILL.md)에서 원본 경로 기록; skillInvocationCard() 순수 빌더.
- 실동작 검증(tmux, dist/jeo): `$ralplan` → "╭── [skill] ──╮ / Skill: ralplan / Path: ~/.agents/skills/ralplan/SKILL.md / Prompt: 25 lines" 카드 실표시 후 [workflow:ralplan] start.
- Verified: tsc 0; bun test 1237 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] 라운드 15 후속 — jeo 실동작 e2e 검증 (디스커버리 수리분)
- dist 재빌드(라운드 15 포함) 후 실제 `jeo models` 라이브 실행: openai (oauth) 3 models — gpt-5.5/5.4/5.4-mini가 "Live models" 섹션에 실표시(이전: 죽은 400 경로 → 카탈로그 폴백 2개), gemini (api_key) 21 models — 페이지네이션 완전 리스트(3.5-flash/3.1-pro-preview 포함, lyria/nano-banana 누수 0), anthropic 9·antigravity 8·ollama 1 무손상.
- 발견 모델 실호출 e2e: `--model gpt-5.5`(openai OAuth→Codex responses 경로) → done "verified-gpt55" / `--model gemini-3.5-flash`(API key 경로, 2페이지에서 발견되는 최신 모델) → done "verified-gemini35". 디스커버리가 보여주는 id가 실제 콜까지 동작함을 증명.
[2026-06-12] Pass — $gjc init 거부 에러 복구 강화 (refusal ladder 3단계)
- 현장 증상: jeo-code cwd에서 `$gjc init` → "Anthropic declined to answer (safety refusal — no content)" 종료. 기존 2단계 사다리(동일 재전송 → 도구결과 전체 리셋+중립 재개)로도 관통되는 케이스.
- 분석: 도구 결과가 전부 제거된 뒤에도 거부가 지속되면 잔여 트리거 후보는 시스템 프롬프트에 주입된 저장소발 가이던스(<project_context> — AGENTS.md/rules의 공격적 문구가 OAuth 분류기를 작동시킴).
- 수정: MAX_REFUSAL_RETRIES 2→3; 3단계 = 시스템 프롬프트에서 <project_context>…</project_context> 블록만 제거(코어 지시 보존, 교체-비변이로 identity 캐시 정합) 후 동일 스텝 재시도. 제거할 블록이 없으면 동일 과금 호출을 태우지 않고 즉시 친화 에러로 종료.
- Tests: 3단계 회복 성공(거부×3→스트립 후 done, 시스템 프롬프트 잔존 확인, 호출 4회 정확) / 블록 부재 시 3호출 후 에러(추가 과금 0).
- 실동작: jeo-code cwd `$gjc init` 재실행 — 7스텝+ 정상 진행(도구 5 ok), 에러 미발생.
- Verified: tsc 0; bun test 1239 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] 라운드 16 (cycle 26) — /exit·^C^C 즉시 종료 (종료 버퍼링 제거)
- 원인: 인터랙티브 REPL 종료 꼬리의 `await distillSessionMemory(...)`가 최종 LLM 콜(최대 20s 타임아웃)을 블로킹 — /exit와 더블 ^C(→/exit 주입) 모두 같은 꼬리를 타므로 종료가 수 초~20초 버퍼링.
- 수정: 증류를 분리된 백그라운드 자식으로 강등 — spawnDetachedDistill이 페이로드(.jeo/memory/pending-distill-*.json)를 쓰고 숨은 CLI 워커 `jeo memory-distill <file>`을 스폰 후 즉시 반환. distillInvocation은 tmuxLaunchCommand와 동일한 3형태 자기호출(컴파일 가상경로/소스/심). **핵심**: Bun.spawn이 아니라 node:child_process `detached: true`(자체 세션/프로세스 그룹) — 부모 pane이 닫히면 같은 그룹의 자식이 죽는 것을 라이브로 관측하고 교정.
- 라이브 검증(tmux 인터랙티브, dist/jeo): 턴 1회 후 /exit → **종료 지연 ≈ 0.1s**(이전 최대 20s), 종료 직후 pending 페이로드 존재 → 12초 후 자식이 MEMORY.md 작성+페이로드 정리(터미널 사망 생존). hermes 루프 유지, 사용자 시계에서만 제거.
- 부수 수리: themes.test.ts가 실 ~/.jeo/config.json의 테마(blue-crab)를 읽어 깨지던 격리 버그 — fallback 분기 호출에 빈 JEO_CONFIG_DIR 주입.
- Verified: typecheck 0; full bun test 1239+ pass / 0 fail; 신규 테스트 4종(memory: invocation 3형태/스폰 계약+unref/게이트/워커 e2e).
[2026-06-12] Pass — gjc 카드 패리티: 클립 ⟦Ctrl+O for more⟧ 힌트 + 코드 하이라이트 + Ctrl+O 도구 출력 전문
- 스크린샷(gjc running-tool 카드) 반영 3종: ① 카드 클립 라인이 "… N more lines ⟦Ctrl+O for more⟧"(ASCII [Ctrl+O for more])로 — 숨긴 내용의 도달 경로를 명시; ② 코드 카드(bash/ts/js/py/json/yaml/rust/go) 본문에 lightHighlightLine 경량 하이라이트(주석·문자열·키워드) — 랩핑 후 적용으로 폭 계산 무손상, color:false 바이트 동일; ③ Ctrl+O 상세 뷰에 "detail · full last tool output (tool)" 섹션 추가 — withToolDetailCapture 이벤트 래퍼가 TUI/plain 양 싱크에서 마지막 도구의 전체(비절단) 출력을 캡처(2000행 캡 + sanitizeForTerminal), 응답이 없어도 도구 출력만으로 열람 가능.
- 실동작: bash 카드 `$ seq 1 40` 렌더 확인; 클립 힌트·하이라이트는 단위 계약으로 고정(chalk 레벨 강제하에 ANSI 적용/미적용 검증).
- Verified: tsc 0; bun test 1249 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] Pass — 클립 힌트 실검증 갭 수정 (summarize-stage 마커)
- 실동작 검증에서 발견: bash 결과 카드의 "… 36 more line(s)"는 previewLines(요약 단계) 클립이라 박스 단계 힌트가 안 붙었음. formatForgeBox가 요약 단계 마커를 감지해 유니코드 인지 힌트를 재작성 — 두 클립 레이어 모두 ⟦Ctrl+O for more⟧ 보장.
- 실기기 증거(dist/jeo, tmux): ① 카드에 "… 36 more line(s) ⟦Ctrl+O for more⟧" 실표시, ② Ctrl+O → "detail · full last tool output (bash)" + seq 1 40 전체(39·40 포함) 열람 — 카드에서 잘린 36줄 전부 도달.
- Verified: tsc 0; bun test 1250 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] Pass — Todos 미갱신 고착 수정 (done-시점 reconcile 게이트)
- 현장 증상: 28스텝 [DONE] 턴이 Todos "✓0 ◐1 ·4 / 5"로 종료 — 프로토콜 문구("Resend the FULL list, marking progress")만 있고 강제 장치가 없어 모델이 초기 계획 후 상태 갱신을 전부 생략.
- 수정: AgentLoopEvents.onBeforeDone(reason) 훅 — 단독 done 도착 시 1회 한정 바운스(엔진이 beforeDoneNudgeUsed로 무한루프 차단, 두 번째 done은 무조건 통과하는 escape hatch). launch가 턴별 todo 스냅샷(turnTodos)으로 미완료 항목을 열거하는 reconcile 메시지를 공급 — "FULL 리스트를 done 마킹으로 재전송 후 done 재호출".
- Tests: 바운스→todo 갱신→done 수락(정확히 3호출), 완고한 done 2회째 통과(훅 1회만 소비), 깨끗한 플랜 즉시 종료(추가 비용 0).
- 실동작(dist/jeo, tmux): 모델에 "todo 갱신 없이 done" 지시 → 게이트 바운스 → "Reconciled the todo list…" → 최종 Todos ✓2/2.
- Verified: tsc 0; bun test 1253 pass / 0 fail; dist/jeo 재빌드.
[2026-06-12] Pass — 서브에이전트 시스템화(하드코딩 제거) + TUI mid-turn Ctrl+O
- 서브에이전트 레지스트리 시스템화: config.subagents에 title/description/prompt/readOnly 선언만으로
  커스텀 역할이 1급 역할로 등록 (rolesFromConfig/allSubagentRoles). 번들 4종은 기본값일 뿐 —
  역할 목록·프롬프트·도구셋·스텝 한도 전부 데이터 구동. 미지 id의 단순 model 핀은 역할로 승격
  안 함(오타 안전). 커스텀 역할 기본 read-only(readOnly:false 명시 시만 mutate).
- 소비처 전환: task-tool(역할 해석+프로토콜 라인 동적화 taskToolProtocolLine(config)), /agents
  패널·편집기, apply-target 픽커, approve/team 플랜 게이트, team 워커 — 전부 병합 레지스트리 사용.
- Ctrl+O TUI 동작: 라이브 턴 중 \u000f가 buffered input으로 삼켜지던 것을 하니스 onDetailKey로
  인터셉트 → tui.showDetail()이 ledger에 detail 블록 flush. 프롬프트(readline) 경로와 동일한
  composeDetailLines 공용 컴포저. 붙여넣기/임베디드 \u000f는 데이터로 유지.
- Tests: custom-subagent-roles(6) — 선언≠핀 구분, read-only 기본, 도구셋, 템플릿 치환, 표면 노출,
  번들 중복 금지; ctrl-o-detail(3) — 단독 키만 발화, 페이스트/임베디드 불발화, 훅 없으면 불활성.
- 실동작(dist/jeo, tmux 샌드박스): ① 클립 카드 "… 36 more line(s) ⟦Ctrl+O for more⟧" 표시 중
  Ctrl+O → "detail · full last tool output (bash)" 전체 덤프 확인. ② config에만 선언한 reviewer
  역할 위임 → [AGENT] reviewer ▸ start → step 1/6 → ◂ done 카드 ✓.
- 동시 패스 조정: 45b7b19가 본 세션 subagents.ts를 선커밋, README 동기화로 npm 토큰 섹션 누락
  (install-script 테스트 fail) → aa15387이 4개 언어 복원, 본 패스 게이트에서 green 확인.
- Verified: tsc 0; bun test 1262 pass / 0 fail; dist/jeo 재빌드.

[2026-06-12] Pass — Ctrl+O reversible history panel
- 요청: Ctrl+O로 history/detail을 열고, 다시 Ctrl+O로 닫을 수 있게 개선.
- 원인: 기존 mid-turn Ctrl+O는 detail을 scrollback ledger로 append만 해서 닫을 수 없었고, prompt 대기 경로도 detail을 콘솔에 덤프했다가 footer를 복구하는 one-shot 동작이었다.
- 수정: LaunchTui에 historyLines 상태 + renderHistoryPanel() 추가. showDetail()이 first press=open, second press=close 토글로 동작하고, live inline frame 안에 `history · Ctrl+O closes` 패널을 렌더한다(스크롤백 오염 없이 닫힘).
- prompt 대기 경로도 promptHistoryLines + historyPreviewLines()로 fixed footer reservation 안에 같은 history 패널을 그리고, 두 번째 Ctrl+O 또는 입력 편집 시 닫는다.
- 힌트바 라벨을 `^O history`로 변경. Tab completion 보정(중간 `/`·`$` 토큰 완성 시 줄 앞부분 보존)과 테스트도 함께 반영된 현재 워킹트리로 검증.
- Test hardening: todo-card의 유니코드 기대 테스트를 TERM/LANG에 의존하지 않도록 UTF-8 터미널 환경으로 고정.
- Tests: Ctrl+O live-frame 토글 단위 테스트 추가; focused `tui-app/ctrl-o-detail/todo-card` green. Full gate: tsc 0; bun test 1263 pass / 0 fail; dist/jeo rebuild.
- Live verification (compiled dist/jeo, tmux): live turn Ctrl+O open → `history · Ctrl+O closes` visible, Ctrl+O close → gone. Prompt wait Ctrl+O open/close도 동일하게 current pane에서 확인.

[2026-06-12] Pass — Ctrl+C hard-exit (terminal force quit)
- Request: make Ctrl+C force-terminate jeo instead of soft-clearing/aborting and requiring a second press.
- Root cause: prompt path treated Ctrl+C as line-editor behavior (clear input / graceful `/exit` fallback), while live-turn harness only aborted first and hard-exited on the second press. Bun/tmux can deliver Ctrl+C as readline SIGINT, process SIGINT, a raw `\u0003` byte, or a readline close that later resolves `/exit`; all paths must converge on exit 130.
- Fix: createInFlightAbortHarness.handleSigint now aborts observers and immediately calls onHardExit. REPL prompt registers a shared forceExitFromCtrlC on readline SIGINT, process SIGINT, stdin data `\u0003`, unexpected interactive readline close, and raw text containing `\u0003`.
- Terminal hygiene: prompt raw mode is owned/restored for the REPL lifetime; force-exit and normal-exit paths restore raw mode and cursor before exit.
- Follow-up fix found by full gate: jeoEnv now reads only canonical `JEO_*`; test wording/fixtures corrected.
- Tests: launch-toggles Ctrl+C contracts updated; env-helper verifies JEO empty-string semantics.
- Live verification (compiled dist/jeo, tmux, accurate `sh -lc` exit-code wrapper): prompt Ctrl+C => EXIT:130; live turn Ctrl+C while agent/tool turn active => EXIT:130.
- Verified: tsc 0; bun test 1269 pass / 0 fail; dist/jeo rebuilt.
[2026-06-12] Pass — canonical jeo-only cleanup
- Removed every remaining legacy spelling from source, docs, plans, tests, runtime guidance, generated transcripts/artifacts, and path names; `search`/`find` over the entire project (including ignored hidden state) now returns zero legacy-name hits.
- Removed the old binary alias from package/install/uninstall surfaces; runtime env lookup now accepts only canonical `JEO_*`.
- `/agents` now prints the subagent roster without opening the interactive picker; the picker is explicit via `/agents edit`, so real `jeo` PTY monitoring can list subagents and exit cleanly.
- Test isolation hardened for bash/subagent bash tests so active local `.jeo` workflow state cannot leak into full-suite order.
- Verified: focused tests pass; `bun run typecheck` pass; `bun test` 1279 pass / 0 fail; `bun build src/cli.ts --compile --outfile dist/jeo` pass; compiled `jeo --help`, `jeo mcp tools`, and PTY `/agents` runtime checks show only `jeo` surfaces.
